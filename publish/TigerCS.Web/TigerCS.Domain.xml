<?xml version="1.0"?>
<doc>
    <assembly>
        <name>TigerCS.Domain</name>
    </assembly>
    <members>
        <member name="T:TigerCS.Domain.Audit.AuditEntry">
             <summary>
             Append-only audit trail row (ADR-0018, MVP-Data-Dictionary.md §2.22).
             Deliberately no FK on <see cref="P:TigerCS.Domain.Audit.AuditEntry.EntityId"/> — AuditEntries must be able
             to record an action against any current or future entity type without a
             schema change per new type (MVP-ERD.md §2.22).
            
             <para>
             <b>Reconciliation with the approved ERD and the future S-05 module.</b>
             This is not a CRM-owned, temporary audit schema that S-05 will later
             replace — the columns below are the exact canonical shape from
             MVP-Data-Dictionary.md §2.22 (table name, every column, and the
             deliberate no-FK-on-EntityId design), not an approximation. Backlog
             S-05 ("Audit trail, Outbox, and idempotency foundation") is three
             things bundled as one backlog item: (1) an audit-writing mechanism used
             by every later feature, (2) an Outbox dispatch loop, (3)
             <c>IdempotencyRecords</c>. Only (1) is built here, using the real,
             final table — writing an <see cref="T:TigerCS.Domain.Audit.AuditEntry"/> row through
             <c>IAuditEntryWriter</c> in the same DB transaction as the state change
             it describes, per ADR-0018, with no update/delete path at any privilege
             level. (2) and (3) remain S-05's scope; when that work lands it extends
             this table's consumers (e.g. Identity and Access's own login/logout
             audit, deferred in PR #9), it does not migrate or rename it.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Infrastructure.IdempotencyRecord">
             <summary>
             MVP-Data-Dictionary.md §2.23 / MVP-ERD.md §2.23 / ADR-0014 — the
             generalized idempotency record, deliberately its own table rather than a
             bare column on one consumer, so the same mechanism covers the Outbox
             dispatcher, Genesys webhook dedup, and the SLA breach check.
            
             <para>
             <b>What this increment uses it for.</b> SLA-Architecture.md §15: "Every
             breach/warning/escalation check carries an idempotency key
             (<c>TicketId + CheckType + DueTimestamp</c>) so the scheduled job and the
             sweep never produce a duplicate notification for the same due event."
             Both detection paths of §13/§14 target the same key, so whichever arrives
             second is a no-op. The unique index on
             <see cref="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.IdempotencyKey"/> is what makes that true under genuine
             concurrency rather than only under sequential retries — a lost race
             surfaces as a unique-constraint violation, which the caller treats as
             "already processed".
             </para>
            
             <para>
             <b><see cref="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.ExpiresAtUtc"/> is never set by this increment.</b>
             §2.23 marks the retention window <c>[ASSUMPTION — window not yet
             specified]</c>, and purging breach-check records early would let a
             re-delivered deadline check duplicate a breach. The column exists because
             it is part of the approved shape; the housekeeping job that would consume
             it is not in this increment's scope.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.IdempotencyKey">
            <summary>Unique across the whole table (MVP-Data-Dictionary.md §2.23). Scope is part of the composed key's text, so two scopes cannot collide.</summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.Scope">
            <summary>"OutboxDispatch" / "GenesysWebhook" / "SlaBreachCheck" — see <see cref="T:TigerCS.Domain.Infrastructure.IdempotencyScopes"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.ResultReference">
            <summary>Lets a duplicate request return the same prior result rather than recomputing (MVP-Data-Dictionary.md §2.23).</summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.ExpiresAtUtc">
            <summary>Housekeeping only, and never populated here — see this type's remarks.</summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.IdempotencyRecord.MarkSeenAgain(System.DateTime)">
            <summary>Records that the same key was seen again. <see cref="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.FirstSeenAtUtc"/> and <see cref="P:TigerCS.Domain.Infrastructure.IdempotencyRecord.ResultReference"/> are never overwritten — the first outcome is the one a duplicate must observe.</summary>
        </member>
        <member name="T:TigerCS.Domain.Infrastructure.IdempotencyScopes">
            <summary>The <c>Scope</c> values of MVP-Data-Dictionary.md §2.23, named once so no call site spells one differently.</summary>
        </member>
        <member name="F:TigerCS.Domain.Infrastructure.IdempotencyScopes.SlaBreachCheck">
            <summary>The SLA breach check of SLA-Architecture.md §15.</summary>
        </member>
        <member name="F:TigerCS.Domain.Infrastructure.IdempotencyScopes.OutboxDispatch">
            <summary>
            ADR-0013/ADR-0014's Outbox dedup. The key is composed at <i>enqueue</i>
            time from <c>TicketId + EventType + EventVersion</c>, inside the same
            transaction as the business state change, so the unique index makes
            "one logical event produces at most one Outbox message" a database
            fact rather than an application convention.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Infrastructure.OutboxMessage">
             <summary>
             MVP-Data-Dictionary.md §2.23 / MVP-ERD.md §2.23 / ADR-0013 — the
             transactional Outbox row. Written in the <i>same database transaction</i>
             as the business state change that produced it, so the state change and
             the intent to notify are atomic and the dual-write problem cannot occur
             (NFR-REL-01).
            
             <para>
             <b>Owned by the Infrastructure module</b> (Module-Design.md — "Owned
             data: <c>OutboxMessage</c>"), not by Notifications and not by Ticketing.
             Ticketing writes one through <c>IOutboxWriter</c>, which is one of
             Infrastructure's four public interfaces; Notifications consumes it. That
             keeps Module-Design.md's prohibition intact in both directions —
             Ticketing never references Notifications, and Infrastructure never
             references a business-capability module.
             </para>
            
             <para>
             <b>No payload PII.</b> The payload carries identifiers and non-personal
             metadata only; a handler re-reads whatever customer data it needs from
             the database at dispatch time. Security-Architecture.md §11 requires
             contact details be masked or omitted from logs, and an Outbox row is
             long-lived, operator-visible and reproduced into Hangfire's dashboard —
             so the same discipline applies to it, and keeping the payload
             identifier-only means no acknowledgement body, caller number or contact
             name is ever persisted here.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.OutboxMessage.Payload">
            <summary>JSON (MVP-Data-Dictionary.md §2.23). Identifiers and non-personal metadata only — see this type's remarks.</summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.OutboxMessage.IdempotencyRecordId">
            <summary>
            FK → <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.IdempotencyRecord"/>, required (MVP-ERD.md §2.23
            "many:1, Required, Restrict"). Replaces the bare <c>IdempotencyKey</c>
            string column of the superseded Architecture-Design.md sketch, so the
            one mechanism also covers Genesys webhook dedup and the SLA
            sweep/scheduled-job overlap (ADR-0014).
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.OutboxMessage.IdempotencyRecord">
             <summary>
             Navigation to the reservation above.
            
             <para>
             <b>Present because the FK genuinely requires it, not for convenience.</b>
             <c>IdempotencyRecords.IdempotencyRecordId</c> is a store-generated
             bigint (MVP-Data-Dictionary.md §2.23), so when the record and the
             message are added to one unit of work — which ADR-0013 requires, since
             both must commit inside the business transaction — the record's key
             does not exist until its INSERT runs. Only a tracked relationship lets
             EF order the two inserts and propagate the generated key into this
             message's FK. Without it the message would insert with
             <c>IdempotencyRecordId = 0</c> and violate the foreign key.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Infrastructure.OutboxMessage.Attempts">
             <summary>
             Incremented once per dispatch attempt, <i>before</i> the attempt runs.
            
             <para>
             <b>This is also the concurrency token</b> that stops two workers from
             delivering the same message (see this type's
             <see cref="M:TigerCS.Domain.Infrastructure.OutboxMessage.BeginAttempt"/>). MVP-Data-Dictionary.md §2.23's
             <c>Status</c> enum has no "InProgress" value, so a claim cannot be
             modelled as a status transition without inventing one; a
             compare-and-swap on this already-approved column achieves the same
             exclusion with no schema invention.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.BeginAttempt">
             <summary>
             Claims this message for one dispatch attempt by incrementing
             <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.Attempts"/>.
            
             <para>
             <b>Why an increment is a claim.</b> <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.Attempts"/> is mapped
             as an optimistic-concurrency token, so persisting this change emits
             <c>UPDATE OutboxMessages SET Attempts = @new WHERE OutboxMessageId =
             @id AND Attempts = @original</c>. Two workers that both read the same
             pending row therefore race on one atomic conditional update: exactly
             one affects a row, and the loser's save throws, which the dispatcher
             treats as "somebody else has this one" and skips. Without it, two
             Hangfire workers (or one worker and a manual re-run) would each send
             the same acknowledgement.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.MarkProcessed(System.DateTime)">
            <summary>Terminal success. <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.LastError"/> is deliberately preserved — the record of a transient failure that later succeeded is diagnostically useful and nothing in §2.23 says to clear it.</summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.RecordTransientFailure(System.String,System.Int32,System.DateTime)">
            <summary>
            A failure that may succeed on a later attempt. The message stays
            <see cref="F:TigerCS.Domain.Infrastructure.OutboxMessageStatus.Pending"/> and remains eligible for
            the dispatcher's next pass, unless this attempt exhausted
            <paramref name="maxAttempts"/> — in which case it dead-letters here
            rather than retrying forever (FR-NOT-05, MVP-API-Contracts.md §6.1).
            </summary>
            <returns><c>true</c> when a further attempt remains, <c>false</c> when this call dead-lettered the message.</returns>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.DeadLetter(System.String,System.DateTime)">
            <summary>
            Terminal failure — retried out, or a failure no retry could fix (a
            recipient that cannot be resolved). The row is <b>kept</b>, never
            deleted: FR-NOT-05 requires a failed send stay visible in an
            operational queue with a dead-letter path, and ADR-0013's whole point
            is that nothing is lost.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.NextEligibleAtUtc(System.DateTime,System.Int32,System.TimeSpan)">
             <summary>
             The earliest moment this message may be attempted again, given how
             many attempts it has already had.
            
             <para>
             <b>Derived, not stored.</b> System-Architecture.md requires "retry
             with backoff", but MVP-Data-Dictionary.md §2.23 has no
             <c>NextAttemptAtUtc</c> column and this increment does not invent
             one. The schedule is instead a pure function of two approved columns
             — <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.OccurredAtUtc"/> and <see cref="P:TigerCS.Domain.Infrastructure.OutboxMessage.Attempts"/> — which
             makes it deterministic, queryable, and identical however many
             processes compute it.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.CumulativeBackoff(System.Int32,System.TimeSpan)">
            <summary>
            Sum of an exponentially doubling delay over <paramref name="attempts"/>
            completed attempts: <c>base·(2^attempts − 1)</c>. With a 30-second
            base that is 0s / 30s / 90s / 210s / 450s before attempts 1–5 —
            increasing gaps, and the whole schedule bounded because
            <c>Attempts</c> itself is bounded by the dead-letter threshold.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxMessage.Truncate(System.String)">
            <summary>LastError is nvarchar(2000) (MVP-Data-Dictionary.md §2.23); a provider exception message can exceed that.</summary>
        </member>
        <member name="T:TigerCS.Domain.Infrastructure.OutboxMessageStatus">
            <summary>MVP-Data-Dictionary.md §2.23: "Pending/Processed/DeadLettered". No "Failed" value exists — a retryable failure stays <see cref="F:TigerCS.Domain.Infrastructure.OutboxMessageStatus.Pending"/> with <c>Attempts</c>/<c>LastError</c> recorded.</summary>
        </member>
        <member name="T:TigerCS.Domain.Infrastructure.OutboxEventTypes">
            <summary>
            The <c>EventType</c> values MVP-API-Contracts.md names, spelled once so no
            producer and consumer can disagree on the string.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Infrastructure.OutboxEventTypes.TicketCreated">
            <summary>MVP-API-Contracts.md §3.1 and §2.6 — "drives the automated acknowledgement notification".</summary>
        </member>
        <member name="F:TigerCS.Domain.Infrastructure.OutboxEventTypes.TicketCreatedVersion">
            <summary>
            The <c>EventVersion</c> component of ADR-0014's outbox idempotency key
            (<c>TicketId + EventType + EventVersion</c>). Bumping it for a given
            event type deliberately makes a re-issued event a distinct logical
            event rather than a suppressed duplicate.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Infrastructure.OutboxEventTypes.IdempotencyKeyFor(System.String,System.Int64,System.Int32)">
            <summary>ADR-0014's key shape, composed in one place so producer and dedup check cannot drift apart.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.ClassificationAndRouting.Category">
            <summary>
            MVP-Data-Dictionary.md §2.5 — Classification and Routing module
            ownership (Module-Design.md). Every category routes to exactly one
            department; that department becomes a ticket's immutable
            <c>OriginatingDepartmentId</c> at creation (FR-CLS-01/FR-RTE-01).
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.ContactReference">
            <summary>
            CRM cache row for a unit's contacts (owner/tenant/representative) —
            never master data (ADR-0006, MVP-Data-Dictionary.md §2.7).
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.CustomerVerification.ContactReference.AuthorizedRepresentativeOfContactReferenceId">
            <summary>Self-ref — supports ISSUE-007's CRM-recorded-authorization requirement.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.ContactReference.RefreshFromCrm(System.String,System.String,TigerCS.Domain.Modules.CustomerVerification.ContactType,System.Nullable{System.Int32},System.DateTime)">
            <summary>Refreshes the display cache from a fresh CRM read — never becomes the authoritative record (ADR-0006).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.ContactType">
            <summary>MVP-Data-Dictionary.md §2.7 (ContactReferences.ContactType).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.UnitReference">
            <summary>
            CRM cache row — never master data (ADR-0006, MVP-Data-Dictionary.md §2.7).
            Stores only the CRM-issued identifier plus a refreshable,
            non-authoritative display cache used for lookups and read-back during
            verification. The CRM itself remains the sole source of truth.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.UnitReference.RefreshFromCrm(System.String,System.String,System.String,System.String,System.DateTime)">
            <summary>Refreshes the display cache from a fresh CRM read — never becomes the authoritative record (ADR-0006).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod">
            <summary>
            How a requester's identity/match was confirmed — deliberately
            channel-neutral (see <see cref="T:TigerCS.Domain.Modules.CustomerVerification.VerificationSession"/>'s remarks). This
            pilot phase's only caller uses <see cref="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.ManualAgentConfirmation"/>
            (an agent's verbal read-back over the phone, FR-VER-03), but the
            domain, API, persistence, and audit trail all support every value below
            without change — a future channel supplies its own value, not a new
            concept.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.ManualAgentConfirmation">
            <summary>An agent confirmed the match directly with the caller (this pilot's only phone-channel method, FR-VER-03's verbal read-back).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.AuthenticatedDigitalUser">
            <summary>The requester was already an authenticated digital user (e.g. a logged-in portal/app session) at the point of confirmation.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.Otp">
            <summary>Confirmed via a one-time passcode sent to a CRM-recorded contact channel.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.FaceToFaceDocumentCheck">
            <summary>Confirmed in person against a physical identity/ownership document (e.g. a kiosk or front-desk channel).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.Other">
            <summary>Any confirmation method not covered by the named values above.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.VerificationSession">
             <summary>
             A short-lived, pre-ticket record of an agent's CRM unit/contact lookup
             and the requester's confirmation of that match (MVP-ERD.md §2.24,
             Finding DR-01 — resolves the circular dependency where ticket creation
             needed a confirmation that itself needed a ticket to exist).
             Single-agent-owned, single-use, write-once snapshot; a ticket is
             created *from* a confirmed, unconsumed session in a later phase
             (Ticketing module, out of scope here).
            
             <para>
             <b>Channel-neutral by design.</b> This entity does not assume Phone or a
             verbal confirmation — <see cref="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Confirm(System.DateTime,TigerCS.Domain.Modules.CustomerVerification.VerificationMethod)"/> models "the requester
             confirmed this match, by this <see cref="T:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod"/>,"
             regardless of which channel obtained it. MVP's own approved scope is
             Phone-only (FR-CH-01/FR-CH-02) and this pilot's only caller supplies
             <see cref="F:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod.ManualAgentConfirmation"/>
             (a verbal read-back per FR-VER-03), but both the wire contract
             (MVP-API-Contracts.md §2.4.3's <c>Confirmed</c>/<c>VerificationMethod</c>
             fields) and this domain method are channel-neutral by construction, not
             merely by convention — a future channel (App/Website, WhatsApp, Social
             Media, kiosk — Solution-Analysis.md §2.2's FR-CH-02/03, Phase 2+) supplies
             its own <see cref="T:TigerCS.Domain.Modules.CustomerVerification.VerificationMethod"/> value to the
             same <see cref="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Confirm(System.DateTime,TigerCS.Domain.Modules.CustomerVerification.VerificationMethod)"/> call, without any change to this entity, its
             state machine, or the API shape.
             </para>
             <para>
             For this pilot phase (MVP-Implementation-Backlog.md §0.2/S-07), the
             application layer combines unit/contact selection and confirmation into
             a single call, rather than MVP-API-Contracts.md §2.4's separate
             start/select/confirm endpoints — but this entity still models the full
             state machine, so the single-use/expiry/ownership invariants are real,
             not simulated, and ready for the Ticketing module to consume unchanged.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Confirmed">
            <summary>Whether the requester confirmed this unit/contact match — channel-neutral (see the type-level remarks).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.VerificationMethod">
            <summary>How the confirmation above was obtained. Set only at <see cref="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Confirm(System.DateTime,TigerCS.Domain.Modules.CustomerVerification.VerificationMethod)"/> time; null beforehand.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.IdempotencyKey">
            <summary>
            Client-supplied idempotency key for the combined create+confirm call
            (S-07) — a pilot-scoped, module-local substitute for the generalized
            IdempotencyRecords mechanism (ADR-0014 / backlog S-05), which has not
            been built yet. Null when the caller didn't supply one.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.EffectiveStatus(System.DateTime)">
            <summary>
            Reflects Expired even if a background expiry sweep hasn't run yet
            (MVP-ERD.md §2.24) — computed at read time, never persisted implicitly.
            Applies while InProgress or Confirmed-but-not-yet-consumed; a session
            already Consumed is a settled historical fact and is never
            retroactively treated as expired.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Confirm(System.DateTime,TigerCS.Domain.Modules.CustomerVerification.VerificationMethod)">
            <summary>
            Records the requester's confirmation of the unit/contact match and
            the method used to obtain it (MVP-API-Contracts.md §2.4.3, folded
            into S-07's single call). Channel-neutral — see the type-level
            remarks for why neither this method nor <paramref name="verificationMethod"/>
            is scoped to a verbal/phone confirmation specifically, even though
            MVP's own approved channel is Phone-only.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.Consume(System.Int64,System.DateTime)">
            <summary>
            Marks the session consumed by a ticket — single-use (MVP-ERD.md
            §2.24). Not called by any endpoint in this phase (ticket creation is
            out of scope, per MVP-Implementation-Backlog.md S-08); implemented and
            unit-tested now so the invariant is real and ready for the Ticketing
            module to call unchanged.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSession.MarkExpired(System.DateTime)">
            <summary>Explicit sweep transition, mirroring the Hangfire sweep pattern (ADR-0015) other expiring entities use.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.VerificationSessionException">
            <summary>Base type for violations of the VerificationSession state machine (MVP-ERD.md §2.24).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.CustomerVerification.VerificationSessionException.#ctor(System.String)">
            <summary>Base type for violations of the VerificationSession state machine (MVP-ERD.md §2.24).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.CustomerVerification.VerificationSessionStatus">
            <summary>MVP-Data-Dictionary.md §2.24 (VerificationSessions.Status).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.AuthorizationOverride">
             <summary>
             The single, solution-wide definition of the platform-administration
             authorization override.
            
             <para>
             <b>Confirmed management decision (2026-08-21), superseding the previous
             exclusion.</b> Solution-Analysis.md §4.1's permission matrix originally
             granted <see cref="F:TigerCS.Domain.Modules.IdentityAndAccess.Roles.SystemAdministrator"/> only "View: All
             (technical)", "Export: All" and "Admin: Full" — every operational column
             (Create/Edit/Assign/Transfer/Escalate/Resolve/Close) was a dash. That
             exclusion is superseded: the System Administrator role now passes every
             application authorization policy. See ADR-0024 and
             Security-Architecture.md §2.1.
             </para>
            
             <para>
             <b>This is an authorization override, and nothing else.</b> It answers
             only "may this caller perform this action at all?" — the question
             ADR-0005's policies exist to answer. It never suppresses request
             validation, ticket status-transition rules, closed-ticket immutability,
             optimistic-concurrency checks, database constraints, required business
             data, or audit writes: those are enforced downstream of every
             authorization decision and are reached, unchanged, by an overridden
             caller. Two further carve-outs are deliberate and documented where they
             live: session validity (<c>ActiveEmployeeRequirement</c>, an
             <c>IIdentityGateRequirement</c> — a deactivated administrator is still
             rejected, per Security-Architecture.md §14/FR-ADM-02) and per-record
             verification-session ownership (MVP-ERD.md §2.24 — see ADR-0024's
             "Business rules the override does not reach").
             </para>
            
             <para>
             <b>Why a constant and a predicate rather than a role name typed at each
             call site.</b> Requirement 4 of the correction: the override must live in
             one central mechanism so future policies include it automatically and
             safely. Everything that applies the override — the policy-layer
             <c>SystemAdministratorOverrideHandler</c> and the application-layer
             <c>AuthorizationGate</c> — reads it from here, so the set of overridden
             roles is changed in exactly one place.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.IdentityAndAccess.AuthorizationOverride.OverrideRole">
            <summary>The one role that overrides application authorization policies.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.IdentityAndAccess.AuthorizationOverride.AppliesTo(System.Collections.Generic.IEnumerable{System.String})">
            <summary>
            True when the caller holds <see cref="F:TigerCS.Domain.Modules.IdentityAndAccess.AuthorizationOverride.OverrideRole"/>. Ordinal
            comparison, matching how ASP.NET Core Identity persists and compares
            <c>AspNetRoles.Name</c> and how <c>ClaimsPrincipal.IsInRole</c>
            compares the resulting role claims — a case-insensitive match here
            would authorize a role name the identity store itself treats as
            different.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.Department">
            <summary>
            MVP-Data-Dictionary.md §2.3. Departments are never hard-deleted once
            referenced (Categories, Tickets) — IsActive=0 is the only removal path.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.DepartmentAssignmentException">
            <summary>Base type for violations of the department-assignment invariants.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.IdentityAndAccess.DepartmentAssignmentException.#ctor(System.String)">
            <summary>Base type for violations of the department-assignment invariants.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.Employee">
            <summary>
            Domain extension of AspNetUsers (ADR-0004, MVP-Data-Dictionary.md §2.2).
            EmployeeId is both the PK here and the FK to AspNetUsers.Id (identifying
            1:1 relationship) — never a hard delete; DeactivatedAtUtc is the only
            removal path (FR-ADM-02).
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.ICurrentUserContext">
            <summary>
            Reads the acting employee's identity/claims for the current request
            (Module-Design.md's "Identity and Access" public interface). Implemented
            in Infrastructure against the framework's ClaimsPrincipal.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.IEmployeeDirectory">
            <summary>
            Read access to Employee/Department data (Module-Design.md's "Identity and
            Access" public interface) — the seam other modules use instead of
            depending on Infrastructure/EF Core directly.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.Roles">
            <summary>
            The fixed MVP role set (Solution-Analysis.md §4). Role names below use the
            exact display strings approved for this pilot's Identity module: "CS Agent"
            and "CS Supervisor" (rather than the design documents' "Geyness Agent" and
            "Supervisor") per an explicit naming decision made when this increment was
            scoped — recorded formally in ADR-0004's "Pilot Role-Naming Decision"
            section, not only here. All other names match the documents verbatim.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.IdentityAndAccess.Roles.Descriptions">
            <summary>High-level descriptions for GET /api/roles (MVP-API-Contracts.md §1.5).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.IdentityAndAccess.UserDepartmentAssignment">
            <summary>
            Many-to-many Employee&#8596;Department membership, with at most one primary
            department per employee (MVP-ERD.md §0.1, MVP-Data-Dictionary.md §2.4).
            The "exactly one primary row per EmployeeId" invariant is app-enforced
            (see DepartmentAssignmentService) and backed by a filtered unique index
            on (EmployeeId) WHERE IsPrimary = 1.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Notifications.Notification">
             <summary>
             MVP-Data-Dictionary.md §2.21 / MVP-ERD.md §2.21 — one row per outbound
             notification, owned by the Notifications module (Module-Design.md).
            
             <para>
             <b>Distinct from the Outbox row that carries it.</b> The
             <c>OutboxMessages</c> row (§2.23) is the transactional record that <i>an
             effect is owed</i>, written atomically with the business state change
             (ADR-0013). This row is the record of <i>a delivery to one recipient on
             one channel</i> — its status, its address, its retry count. One Outbox
             message could fan out to several of these; MVP fans out to exactly one,
             since the only MVP channel is Email (§8 of MVP-API-Contracts.md, and
             §2.21's own "MVP: Email only").
             </para>
            
             <para>
             <b>No message body is stored here, and none is logged.</b> §2.21 defines
             no body/subject column, and Security-Architecture.md §11 requires contact
             details be masked or omitted from log output. The rendered
             acknowledgement exists only in the value handed to <c>IEmailSender</c>;
             what survives is the delivery <i>fact</i>, which is what FR-NOT-05 asks
             be logged and correlation-tracked.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Notifications.Notification.TicketId">
            <summary>
            Nullable per §2.21 — "Null only for a non-ticket-specific notice
            [ASSUMPTION — at MVP, every notification is ticket-scoped; nullable
            kept for forward compatibility]". Every row this increment writes
            carries a ticket.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Notifications.Notification.RecipientEmployeeId">
            <summary>Null for an external recipient — the customer, via email (§2.21). Always null in this increment: the acknowledgement goes to the requester, never to staff.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Notifications.Notification.RecipientAddress">
            <summary>
            Populated for external/email recipients (§2.21). <b>Null when no
            deliverable address could be resolved</b> from the verified requester
            snapshot — which is a recorded outcome, not a silently dropped
            notification: the row still exists, dead-lettered and visible.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Notifications.Notification.OutboxMessageId">
            <summary>
            FK → <c>OutboxMessages</c>, nullable per §2.21 / MVP-ERD.md §2.21
            ("a <c>Pending</c> row created before dispatch may briefly have no
            Outbox link yet"). Always populated in this increment, because the
            row is created by the dispatcher from an Outbox message that already
            exists.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Notifications.Notification.MarkSent">
             <summary>
             Terminal success.
            
             <para>
             Refuses to run twice. Together with <c>Ticket.RecordAcknowledgementSent</c>'s
             own write-once rule this is the delivery-side half of "a duplicate
             dispatch never sends twice": even if a redelivered Outbox message
             reaches the handler, the already-<see cref="F:TigerCS.Domain.Modules.Notifications.NotificationDeliveryStatus.Sent"/> row cannot be sent
             again.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Notifications.Notification.RecordFailedAttempt">
            <summary>A failed attempt that will be retried — the row stays visible with its retry bookkeeping (FR-NOT-05).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Notifications.Notification.DeadLetter">
            <summary>Retries exhausted, or a failure no retry could fix. Kept, never deleted (FR-NOT-05's dead-letter path).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Notifications.Notification.IsTerminal">
            <summary>True once the delivery reached a state no further attempt may change.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Notifications.NotificationType">
            <summary>MVP-Data-Dictionary.md §2.21: "Acknowledgement/Warning/Breach/Escalation". Only <see cref="F:TigerCS.Domain.Modules.Notifications.NotificationType.Acknowledgement"/> is produced by this increment.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Notifications.NotificationChannel">
            <summary>MVP-Data-Dictionary.md §2.21: "MVP: Email only". SMS/WhatsApp/push are Phase 2 (Solution-Analysis.md §2.7) and deliberately have no value here.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Notifications.NotificationDeliveryStatus">
            <summary>MVP-Data-Dictionary.md §2.21: "Pending/Sent/Failed/DeadLettered".</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendar">
             <summary>
             MVP-Data-Dictionary.md §2.20 / MVP-ERD.md §2.20 / ADR-0010 — the weekly
             working pattern and business-day window consulted by every non-Critical
             due-date computation.
            
             <para>
             Reference data, deliberately not hardcoded: ISSUE-017 approved
             Saturday–Thursday, 08:00–18:00, Friday off, but approved it "explicitly
             still built as configurable data" (ADR-0010) so the working week and
             window can be corrected without a redeploy. A retired calendar is
             superseded by a new effective-dated row, never deleted (MVP-ERD.md §2.20).
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendar.TimeZone">
            <summary>
            The zone <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendar.BusinessDayStartLocal"/>/<see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendar.BusinessDayEndLocal"/>
            and every <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.Holiday.HolidayDate"/> are expressed in. Persisted
            SLA timestamps remain UTC (NFR-UAE-01); this is what the calculation
            converts through, and the only place a local-time concept exists at all.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot">
             <summary>
             An immutable, already-resolved view of one <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendar"/>
             plus its seven working-day rows and its holiday dates — the exact input
             <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator"/> needs and nothing else.
            
             <para>
             It exists so the due-date calculation stays a pure function of its
             arguments (ADR-0010: "a pure function <c>IsBusinessMoment(dateTimeUtc)</c>
             consuming this reference data is trivially unit-testable"). The
             calculator never touches a repository, a clock, or a
             <see cref="T:System.TimeZoneInfo"/> lookup, so every worked example in
             SLA-Architecture.md can be asserted directly against it.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot.TimeZone">
            <summary>The zone the window and holiday dates are expressed in. Resolved once by the caller, so this type performs no environment-dependent lookup.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot.BusinessDayLength">
            <summary>Length of one full working day, used to express a "business day" target in minutes and to bound the walk in <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator"/>.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot.IsWorkingDate(System.DateOnly)">
            <summary>True when <paramref name="localDate"/> is a working weekday for this calendar and is not one of its holiday dates (SLA-Architecture.md §4).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarWorkingDay">
            <summary>
            MVP-Data-Dictionary.md §2.20 — one row per <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarWorkingDay.DayOfWeek"/> value
            (0–6), exactly seven per calendar (MVP-ERD.md §2.20's app-enforced 1:7
            cardinality). Seeded per ISSUE-017: Saturday–Thursday working, Friday off.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarWorkingDay.DayOfWeek">
            <summary>0–6, matching <see cref="T:System.DayOfWeek"/>'s own numbering (Sunday = 0).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType">
            <summary>
            MVP-Data-Dictionary.md §2.17 — <c>TicketEscalations.TriggerType</c>
            ("AutoBreach/AutoWindowExpired/ManualFlag/ManualLevel4").
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.AutoBreach">
            <summary>System-raised, the moment an SLA deadline is detected breached (FR-ESC-02, backlog S-11). Always Level 2 in this pilot — see <see cref="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RaiseAutomaticLevel2OnBreach(System.Int64,System.Byte,System.DateTime)"/>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.AutoWindowExpired">
            <summary>
            The timed Level 2 → Level 3 (GM) advance of ADR-0011/SLA-Architecture.md §12.
            <b>Not produced by any code path in this pilot</b> — MVP-Implementation-Backlog.md
            §0.2 and S-11 both place it out of scope ("the timed window-based Level 2→3
            auto-advance ... is not built in this pilot"), and §2.6 lists it as an
            explicitly-labelled stretch item. The member exists because the column's
            approved value set includes it, not because anything writes it.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.ManualFlag">
            <summary>An agent (or above) manually flagging the ticket for escalation — FR-ESC-01, MVP-API-Contracts.md §5.7.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.ManualLevel4">
            <summary>The Level 4 (Chairman/CEO) escalation, which is manual-only and role-gated to CS Manager/GM (Solution-Analysis.md §7.6, MVP-ERD.md §2.17, MVP-API-Contracts.md §5.7).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.Holiday">
             <summary>
             MVP-Data-Dictionary.md §2.20 / ADR-0010 — a single non-working date
             excluded from every non-Critical SLA computation, unique per calendar.
            
             <para>
             <b><see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.Holiday.ConfirmedByEmployeeId"/> is nullable, and this increment
             treats an unconfirmed holiday as effective.</b> ISSUE-012 splits
             ownership (a System Administrator enters the date; Customer Service/HR
             confirms it), and MVP-API-Contracts.md §5.11 explicitly records that
             "whether an unconfirmed holiday already affects SLA math is itself
             unresolved and called out again here rather than silently decided." No
             holiday-administration endpoint ships in this increment
             (MVP-Implementation-Backlog.md §0.2 puts the live holiday-administration
             UI in stretch), so no row can reach the database unconfirmed except by
             seed or direct operator action — but the calculation still has to answer
             the question for any row it reads. It counts every seeded/entered row,
             which is the safer direction: a genuine public holiday that HR has not
             yet countersigned still stops the clock, rather than silently running an
             SLA through a day the business was closed. Flagged, not decided
             unilaterally: if management resolves ISSUE-012's open question the other
             way, the change is one predicate in
             <c>BusinessCalendarSnapshot.IsWorkingDate</c>.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.Holiday.HolidayDate">
            <summary>A calendar-local date (never a UTC instant) — the whole local day is excluded.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.Holiday.EnteredByEmployeeId">
            <summary>The technical administrator who entered the date (ISSUE-012).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.Holiday.ConfirmedByEmployeeId">
            <summary>The business owner who confirmed it, if the eventual ISSUE-012 answer requires confirmation — see this type's remarks.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.Priority">
            <summary>
            MVP-Data-Dictionary.md §2.6 — fixed, seeded reference data (SLA and
            Escalation module ownership, MVP-ERD.md §2.6). Only the <c>Priorities</c>
            half of §2.6 is modeled in this increment; <c>SlaPolicies</c> (due-date
            targets, warning thresholds, escalation windows) belongs to the SLA and
            Escalation module's own increment (MVP-Implementation-Backlog.md S-09)
            and is out of scope here — Ticketing needs only a valid PriorityId to
            select and route on, not the SLA math that later attaches to it.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.PriorityLevel">
            <summary>
            The fixed MVP priority set (MVP-Data-Dictionary.md §2.6 — "PK — fixed
            set: 1=Critical, 2=High, 3=Medium, 4=Low"). Numeric values match
            <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.Priority.PriorityId"/> exactly so a raw <c>PriorityId</c> can
            be cast to/compared against this enum without a lookup.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason">
             <summary>
             MVP-Data-Dictionary.md §2.15 — <c>TicketSlaInstances.ChangeReason</c>
             ("e.g., InitialCreation/Upgrade/Downgrade").
            
             <para>
             Only <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.InitialCreation"/> is ever written by this increment.
             <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.Upgrade"/> belongs to backlog S-14 (priority upgrade) and
             <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.Downgrade"/> to the post-pilot downgrade-approval workflow —
             which MVP-Implementation-Backlog.md §0 hard-disables for the pilot
             ("Priority is fixed after ticket creation during the pilot. Downgrades
             are not permitted."). Both members exist because the column's approved
             value set includes them (MVP-Data-Dictionary.md §2.15), not because this
             increment produces them; a row carrying either value cannot be created by
             any code path that ships here.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis">
             <summary>
             MVP-Data-Dictionary.md §2.6 — <c>SlaPolicies.ClockBasis</c>
             ("1=24/7, 2=BusinessHours"). Numeric values match that column exactly.
            
             <para>
             The split is a fixed rule, not configurable behaviour:
             SLA-Architecture.md §3 states Critical-tier due timestamps are computed
             by simple duration addition "with <b>no</b> business-hours or
             holiday-calendar exclusion", and §4 states High/Medium/Low walk forward
             counting only time inside the configured business window. Which basis a
             tier uses is configurable data (this column); what each basis <i>means</i>
             is not.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.TwentyFourSeven">
            <summary>Calendar time. The business calendar and holiday list are not consulted at all (SLA-Architecture.md §3).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.BusinessHours">
            <summary>Only time inside the calendar's working-day window counts (SLA-Architecture.md §4).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType">
             <summary>
             The two independent SLA deadlines ADR-0009 requires be tracked as "fully
             independent pairs" — each with its own due timestamp, its own satisfying
             event, and its own breach flag on every <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance"/>
             row.
            
             <para>
             Also the <c>CheckType</c> component of the breach-check idempotency key
             (<c>TicketId + CheckType + DueTimestamp</c>, SLA-Architecture.md §15 /
             ADR-0014), which is why the two deadlines need a name in the domain at
             all rather than being implied by which property a caller reads.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType.FirstResponse">
            <summary>Satisfied only by <c>Ticket.FirstHumanResponseAtUtc</c> — never by the automated acknowledgement, on any channel (ISSUE-019, SLA-Architecture.md §1).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType.Resolution">
            <summary>Satisfied by <c>TicketResolution.ResolvedAtUtc</c> — the moment Resolve sets the outcome. Closure is deliberately <b>not</b> the achievement event (SLA-Architecture.md §2).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator">
             <summary>
             The SLA due-timestamp computation (FR-SLA-01–04, backlog S-09) — a pure
             function of its arguments, with no clock, repository or ambient
             <see cref="T:System.TimeZoneInfo"/> lookup, so SLA-Architecture.md's worked
             examples can be asserted against it directly (ADR-0010).
            
             <para>
             <b>Two bases, per SLA-Architecture.md §3 and §4.</b>
             <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.TwentyFourSeven"/> (Critical) is simple duration
             addition with no calendar or holiday exclusion whatsoever.
             <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.BusinessHours"/> (High/Medium/Low) walks forward
             from the start event counting only minutes inside the calendar's working
             window, skipping non-working weekdays and every <c>Holiday</c> date — a
             direct implementation of §4's pseudocode.
             </para>
            
             <para>
             <b>UTC in, UTC out</b> (NFR-UAE-01). The business window is a local-time
             concept, so the walk converts into the calendar's zone, works there, and
             converts the answer back; nothing local is ever returned or persisted.
             Every returned <see cref="T:System.DateTime"/> carries
             <see cref="F:System.DateTimeKind.Utc"/>.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator.MaximumDaysWalked">
            <summary>
            Bounds the walk so a misconfigured calendar fails loudly instead of
            spinning. Ten years of days comfortably exceeds any real target: the
            longest approved one is Low's 7 business days.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator.ComputeDueAtUtc(System.DateTime,System.Int32,TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis,TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot)">
            <summary>
            Computes one deadline's due timestamp.
            </summary>
            <param name="clockStartAtUtc">The approved SLA clock-start event (ISSUE-001 Option C — ticket creation). Must be UTC.</param>
            <param name="targetMinutes">The tier's target from <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy"/>. Business minutes under <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.BusinessHours"/>; calendar minutes under <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.TwentyFourSeven"/>.</param>
            <param name="clockBasis">Which of §3/§4's two rules applies.</param>
            <param name="calendar">Consulted only under <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.BusinessHours"/>; may be null for a Critical-tier computation.</param>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator.ComputeBusinessHoursDueAtUtc(System.DateTime,System.Int32,TigerCS.Domain.Modules.SlaAndEscalation.BusinessCalendarSnapshot)">
            <summary>
            SLA-Architecture.md §4's walk. A start event outside the window (an
            evening, a Friday, a holiday) consumes nothing until the next working
            window opens — the behaviour §16's Example B demonstrates for its
            First Response figure.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator.ToUtc(System.DateTime,System.TimeZoneInfo)">
             <summary>
             Converts a calendar-local wall-clock moment back to UTC.
            
             <para>
             The approved pilot calendar is Asia/Dubai, which has no daylight
             saving, so no local moment is ever invalid or ambiguous there. The two
             guards below exist because the zone is configurable data (ADR-0010) —
             a calendar pointed at a DST-observing zone must not throw from inside
             an SLA calculation. An invalid (skipped) local time is moved forward
             past the gap; an ambiguous (repeated) one takes the earlier offset,
             the tighter of the two deadlines.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.FirstResponseAlreadyRecordedException">
            <summary>MVP-API-Contracts.md §5.2: `409 first-response-already-recorded` — <c>Ticket.FirstHumanResponseAtUtc</c> is write-once at the ticket level (MVP-ERD.md §2.10).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.FirstResponseAlreadyRecordedException.#ctor(System.Int64,System.DateTime)">
            <summary>MVP-API-Contracts.md §5.2: `409 first-response-already-recorded` — <c>Ticket.FirstHumanResponseAtUtc</c> is write-once at the ticket level (MVP-ERD.md §2.10).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.EscalationLevelCannotBeLoweredException">
            <summary>
            The escalation level a ticket is at only ever rises (Solution-Analysis.md
            §7.6's Level 1 → 4 hierarchy; ADR-0011 models the level as a state, not a
            counter — the retry-count design was explicitly rejected). Without this,
            an automatic Level 2 raised on a later breach would silently de-escalate a
            ticket a CS Manager had already taken to Level 4.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.EscalationLevelCannotBeLoweredException.#ctor(System.Int64,TigerCS.Domain.Modules.Ticketing.EscalationLevel,TigerCS.Domain.Modules.Ticketing.EscalationLevel)">
            <summary>
            The escalation level a ticket is at only ever rises (Solution-Analysis.md
            §7.6's Level 1 → 4 hierarchy; ADR-0011 models the level as a state, not a
            counter — the retry-count design was explicitly rejected). Without this,
            an automatic Level 2 raised on a later breach would silently de-escalate a
            ticket a CS Manager had already taken to Level 4.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.EscalationLevelTriggerMismatchException">
            <summary>
            Level 4 (Chairman/CEO) is manual-only and role-gated to CS Manager/GM
            (Solution-Analysis.md §7.6, MVP-ERD.md §2.17, MVP-API-Contracts.md §5.7).
            A request pairing <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.ManualFlag"/> — which
            any Agent may use — with <c>Level = 4</c> would reach Level 4 without
            passing that gate, so the pairing is rejected outright rather than left to
            the authorization check alone.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.EscalationLevelTriggerMismatchException.#ctor(System.Byte,TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType)">
            <summary>
            Level 4 (Chairman/CEO) is manual-only and role-gated to CS Manager/GM
            (Solution-Analysis.md §7.6, MVP-ERD.md §2.17, MVP-API-Contracts.md §5.7).
            A request pairing <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.ManualFlag"/> — which
            any Agent may use — with <c>Level = 4</c> would reach Level 4 without
            passing that gate, so the pairing is rejected outright rather than left to
            the authorization check alone.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy">
             <summary>
             MVP-Data-Dictionary.md §2.6 (the <c>SlaPolicies</c> half) / MVP-ERD.md
             §2.6 — the per-priority SLA targets, one row per <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.Priority"/>
             (1:1, identifying, seeded at setup, never deleted).
            
             <para>
             <b>Configuration, not code</b> (FR-SLA-01: "Targets are configuration, not
             code"). Nothing in the due-date calculation hardcodes a tier's minutes;
             <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDueDateCalculator"/> receives them from here.
             </para>
            
             <para>
             <b>Two columns are seeded but not consumed by this increment</b>, and are
             present because MVP-Data-Dictionary.md §2.6 defines them as part of this
             table's approved shape — omitting them would only force a second
             migration later. <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.WarningThresholdPercent"/> drives the
             pre-breach warning event of SLA-Architecture.md §10, and
             <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.Level2ToGmWindowValue"/>/<see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.Level2ToGmWindowUnit"/>
             drive the timed Level 2 → Level 3 advance of §12 — both outside this
             increment's scope (MVP-Implementation-Backlog.md §0.2 and S-11). They
             carry the approved defaults so that the work which does consume them
             needs no data change.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.FirstResponseTargetMinutes">
            <summary>The First Response target. Interpreted under <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.ClockBasis"/> — for <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis.BusinessHours"/> these are minutes <i>of business time</i>, not calendar minutes.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.ResolutionTargetMinutes">
            <summary>The Resolution target, interpreted under <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.ClockBasis"/> exactly as <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.FirstResponseTargetMinutes"/> is.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.WarningThresholdPercent">
            <summary>SLA-Architecture.md §10's warning threshold (approved defaults: Critical 50.00, others 75.00). Seeded, not consumed by this increment — see this type's remarks.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.Level2ToGmWindowValue">
            <summary>SLA-Architecture.md §12's Level 2 → GM window magnitude. Seeded, not consumed by this increment — see this type's remarks.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.Level2ToGmWindowUnit">
            <summary>The unit <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.Level2ToGmWindowValue"/> is expressed in (MVP-Data-Dictionary.md §2.6: "1=Minutes, 2=Hours, 3=BusinessDays"). Seeded, not consumed by this increment.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.TargetMinutesFor(TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType)">
            <summary>The target for one deadline type, so callers select by <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType"/> rather than by remembering which property is which.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation">
             <summary>
             MVP-Data-Dictionary.md §2.17 / MVP-ERD.md §2.17 — one row per escalation
             event, manual or automatic. Never deleted.
            
             <para>
             <b><see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.NotifiedRoles"/> is deliberately distinct from
             <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.Level"/></b> — ADR-0011's central point, and the reason this
             row exists rather than the ticket carrying a level alone. "The GM has
             been told, but the Department Head is still the active owner" is a real
             intermediate state a single signal cannot express, so a Critical breach
             notifying the GM does not, by itself, make the ticket Level 3
             (SLA-Architecture.md §11).
             </para>
            
             <para>
             <b>No <c>RaisedByEmployeeId</c> column</b>, because MVP-Data-Dictionary.md
             §2.17's approved column set has none. The acting employee for a manual
             escalation is recorded in <c>AuditEntries.ActorEmployeeId</c>, which is
             what ADR-0018's append-only audit trail is for; adding a second, parallel
             actor column here would duplicate it and put the approved schema and the
             implementation out of step.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.NotifiedRoles">
            <summary>Who the approved recipient matrix says should be told (SLA-Architecture.md §11, Solution-Analysis.md §7.6/§7.8) — recorded here even though notification <i>delivery</i> is out of this increment's scope, because the split from <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.Level"/> is the schema's point.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RespondedAtUtc">
            <summary>Set by MVP-API-Contracts.md §5.8's respond action, which does not ship in this increment — see the PR description's reported contract gap. Present because it is part of §2.17's approved column set.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RespondingEmployeeId">
            <summary>See <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RespondedAtUtc"/>.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RaiseAutomaticLevel2OnBreach(System.Int64,System.Byte,System.DateTime)">
             <summary>
             FR-ESC-02 / backlog S-11 — the automatic Level 2 (Department Head)
             escalation raised the moment an SLA deadline is detected breached.
            
             <para>
             Always Level 2. The timed Level 2 → Level 3 advance is a separate,
             scheduled-job feature that MVP-Implementation-Backlog.md §0.2 and
             S-11 both place outside this pilot, so nothing here can produce a
             Level 3 row; Level 3 and Level 4 are manual-only in this build.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.RaiseManual(System.Int64,System.Byte,TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType,System.DateTime)">
            <summary>MVP-API-Contracts.md §5.7 — a manual escalation. Role gating for <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.EscalationTriggerType.ManualLevel4"/> is an authorization concern and is enforced before this is reached.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.BreachNotifiedRolesFor(System.Byte)">
            <summary>
            SLA-Architecture.md §11's approved breach-recipient matrix (identical
            to Solution-Analysis.md §7.8's): Critical and High notify the
            Department Head and the GM together (ISSUE-004), Medium the Department
            Head, Low the Supervisor.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketEscalation.NotifiedRolesForLevel(System.Byte)">
            <summary>Solution-Analysis.md §7.6's escalation hierarchy: Level 1 the agent's own attempt, Level 2 the Department Head, Level 3 the GM, Level 4 the Chairman/CEO.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance">
             <summary>
             MVP-Data-Dictionary.md §2.15 / MVP-ERD.md §2.15 — one immutable row per
             SLA period a ticket passes through. Never deleted and never overwritten:
             ISSUE-023's explicit requirement is that management reporting can show
             both the original and any changed period for a re-prioritized ticket.
            
             <para>
             <b>Exactly one row per ticket has <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.PeriodEndAtUtc"/> null</b> —
             the current period (MVP-ERD.md §2.15). This increment only ever creates
             that first row (<see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.InitialCreation"/>); closing a
             period and opening a successor belongs to backlog S-14's priority
             upgrade. The filtered unique index in the EF configuration enforces the
             one-current-period invariant at the database, so it cannot be violated by
             a future code path either.
             </para>
            
             <para>
             <b>The breach flags are the highest-consequence integrity rule in this
             schema</b> (MVP-ERD.md §2.15, Architecture-Review-Checklist.md): once
             <c>true</c> they are never reset to <c>false</c> by any code path.
             <see cref="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.MarkBreached(TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType)"/> is the only writer and is one-way; there is no
             setter, no "clear", and no overload that takes a bool.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.PeriodStartAtUtc">
            <summary>The approved SLA clock-start moment for this period (ISSUE-001 Option C — ticket creation, for the initial period).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.PeriodEndAtUtc">
            <summary>Null marks this as the ticket's current period. Only a priority change closes a period (backlog S-14, not this increment).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.FirstResponseBreached">
            <summary>Immutable once true — see this type's remarks. Write via <see cref="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.MarkBreached(TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType)"/> only.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.ResolutionBreached">
            <summary>Immutable once true — see this type's remarks. Write via <see cref="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.MarkBreached(TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType)"/> only.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.ApprovedByEmployeeId">
            <summary>Required only for <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.Downgrade"/>, which no pilot code path can produce (MVP-Implementation-Backlog.md §0 hard-disables downgrades). Never client-supplied on any endpoint (Finding DR-05).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.OpenInitialPeriod(System.Int64,System.Byte,System.DateTime,System.DateTime,System.DateTime)">
            <summary>
            Opens a ticket's first SLA period. The only factory this increment
            exposes: <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.Upgrade"/> rows belong to backlog
            S-14 and <see cref="F:TigerCS.Domain.Modules.SlaAndEscalation.SlaChangeReason.Downgrade"/> rows to the
            post-pilot approval workflow, so neither has a constructor here that
            could be called by accident.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.SlaAndEscalation.TicketSlaInstance.MarkBreached(TigerCS.Domain.Modules.SlaAndEscalation.SlaDeadlineType)">
            <summary>
            Records a breach of one deadline. One-way and idempotent by
            construction: calling it on an already-breached deadline changes
            nothing and returns <c>false</c>, so a caller can tell a first
            detection from a repeat without inspecting the flag itself.
            </summary>
            <returns><c>true</c> if this call is what flipped the flag.</returns>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.Channel">
            <summary>
            FR-CH-02 — a fixed, extensible enum so later channels are additive, not
            a rework. MVP scope is Phone only (FR-CH-01); the other members are
            Phase 2/3 (Solution-Analysis.md §2.2) and are not reachable through any
            endpoint in this increment.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus">
            <summary>
            Solution-Analysis.md §5.2 / MVP-Data-Dictionary.md §2.9. Shared value set
            for both <see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.CrmVerificationStatus"/> and
            <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.VerificationStatus"/> — the same three-state model
            (System-Architecture.md line 182), not two independently-invented enums.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Unverified">
            <summary>No CRM match attempted/found yet.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.PendingCrmVerification">
            <summary>Provisional — awaiting CRM reconciliation (ISSUE-006's outage fallback, this pilot's only trigger).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Verified">
            <summary>CRM unit/contact match confirmed; snapshot captured (FR-VER-05, ADR-0007).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.CustomerLookupSource">
            <summary>The external customer directories <c>CustomerLookupAppService</c> can search — CRM, PACT, Tasleeh.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.DepartmentCustomerLookupSource">
            <summary>
            The Department → customer-lookup-source mapping: which of CRM/PACT/
            Tasleeh <c>CustomerLookupAppService</c> searches for an IntakeRecord
            raised against a given Department. One row per configured source, so a
            Department maps to zero, one, or several sources (e.g. "Department D →
            CRM + Tasleeh" is two rows) — never a hard-coded per-department branch.
            An IntakeRecord with no Department searches every source instead; see
            <c>CustomerLookupAppService.SearchAsync</c>.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.EscalationLevel">
             <summary>
             Tiger-CS-Ticketing-Architecture-Design.md line 324 / Solution-Analysis.md
             §5.3 / §7.6's Agent → Department Head → GM → Chairman/CEO hierarchy. One
             of the five independent lifecycle dimensions (ADR-0008).
            
             <para>
             <b>Independent of <c>TicketStatus</c>, by design</b> — an escalated ticket
             stays Open or InProgress, because escalating it "never removes it from
             active work" (Solution-Analysis.md §7.6). <c>Ticket.RaiseEscalationLevel</c>
             touches this dimension and no other.
             </para>
            
             <para>
             <b>How each level is reached in this pilot.</b> <see cref="F:TigerCS.Domain.Modules.Ticketing.EscalationLevel.Level2"/>
             automatically on an SLA breach, or manually; <see cref="F:TigerCS.Domain.Modules.Ticketing.EscalationLevel.Level1"/> and
             <see cref="F:TigerCS.Domain.Modules.Ticketing.EscalationLevel.Level3"/> manually; <see cref="F:TigerCS.Domain.Modules.Ticketing.EscalationLevel.Level4"/> manually and only by a
             CS Manager or GM (MVP-ERD.md §2.17). The timed Level 2 → Level 3 advance
             of ADR-0011/SLA-Architecture.md §12 is not built here
             (MVP-Implementation-Backlog.md §0.2 / S-11), so nothing advances a level
             on a timer.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.IntakeRecord">
             <summary>
             MVP-ERD.md §2.9 / MVP-Data-Dictionary.md §2.9 — the first, unconditional
             record of any customer interaction, created before any customer lookup
             and before a Ticket exists, so no interaction is ever silently lost
             regardless of whether it turns out to be unit-related, whether a customer
             match is found, or whether it ever becomes a ticket at all (many intake
             attempts never do — e.g. a wrong-number call, a simple information
             request answered verbally).
            
             <para>
             <b>PhoneNumber — captured once, preserved regardless of lookup outcome.</b>
             The identifier used to search for the customer across CRM, PACT, and
             Tasleeh (<c>CustomerLookupAppService</c>). It never changes based on what
             that search finds — Found, NotFound, or Failed all leave it exactly as the
             agent entered it, so a later re-lookup or manual reconciliation always has
             the original value to search with.
             </para>
            
             <para>
             <b>DepartmentId — optional, narrows which sources are searched.</b> When
             set, <c>CustomerLookupAppService</c> searches only the source(s)
             configured for that Department (<see cref="T:TigerCS.Domain.Modules.Ticketing.DepartmentCustomerLookupSource"/>)
             instead of all three. It never gates or restricts anything else — a
             Department-scoped intake is promoted to a Ticket exactly like any other.
             </para>
            
             <para>
             <b>IsUnitRelated — a classification of the interaction/Ticket, not a
             promotion gate, and not fixed at creation.</b> Every intake, unit-related
             or not, may be promoted to a <see cref="T:TigerCS.Domain.Modules.Ticketing.Ticket"/> once a supported Ticket
             Category is selected (<see cref="M:TigerCS.Domain.Modules.Ticketing.IntakeRecord.LinkToTicket(System.Int64,TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus,System.Boolean)"/>). Its real meaning is
             "this interaction is actually associated with a selected Unit" — which
             the current New Ticket wizard cannot know at intake time, because
             identification is deferred entirely to customer lookup (CRM/PACT/Tasleeh),
             run afterward. <see cref="M:TigerCS.Domain.Modules.Ticketing.IntakeRecord.LinkToTicket(System.Int64,TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus,System.Boolean)"/>'s <c>hasSelectedUnit</c>
             parameter is the one explicit, source-agnostic signal that upgrades this
             flag to true — deliberately not inferred from
             <see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.CrmVerificationStatus"/> (a differently-scoped, CRM-named
             concept: whether a customer match was verified, not whether a Unit was
             selected) or from which lookup source produced the match. Upgrade-only:
             an intake already unit-related at creation is never pulled back to false
             by a later Ticket outcome. A final state of <c>IsUnitRelated == false</c>
             with a linked Ticket whose <c>UnitReferenceId</c> is not null never
             occurs through this path.
             </para>
            
             <para>
             <b>RawUnitNumberEntered — optional historical/raw intake information
             only, entirely independent of IsUnitRelated.</b> The unit number exactly
             as the caller said it, before any lookup — useful as an audit/intake note
             when it happens to be captured, but never required for, and never the
             source of, unit-related classification: the authoritative Unit is always
             the one resolved through customer lookup (<c>UnitReferenceId</c> on the
             eventual <see cref="T:TigerCS.Domain.Modules.Ticketing.Ticket"/>), never this raw string. The constructor
             does not couple this field to <c>isUnitRelated</c> in either direction —
             both may be set (or omitted) independently.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.IntakeRecord.LinkToTicket(System.Int64,TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus,System.Boolean)">
             <summary>
             Links this record to the ticket it was promoted into and records the
             resulting customer-match status — write-once (a second promotion
             attempt is a defect, not a valid state, and is rejected by the caller
             before this is ever invoked twice).
            
             <para>
             Every intake — unit-related or not, customer match found or not —
             may be promoted to a ticket once a supported Ticket Category is
             selected. A lookup outcome of NotFound or Failed is recorded here
             exactly like Found; none of the three ever blocks this call.
             </para>
            
             <para>
             <b><see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.IsUnitRelated"/> is upgraded here, never downgraded —
             driven by <paramref name="hasSelectedUnit"/> alone, never by
             <paramref name="resultingStatus"/>.</b> The current New Ticket wizard
             never asks the agent to classify the interaction up front — it always
             creates the intake with <see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.IsUnitRelated"/> false and no
             <see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.RawUnitNumberEntered"/>, deferring identification to
             customer lookup (CRM/PACT/Tasleeh) entirely.
             <paramref name="hasSelectedUnit"/> is the caller's explicit,
             source-agnostic answer to "does the promoted Ticket carry a selected,
             validated Unit reference" (<c>TicketCreationAppService.CreateAsync</c>
             — this method's only caller — passes whether it resolved a real
             <c>UnitReferenceId</c>/<c>ContactReferenceId</c> pair, from whichever
             source produced it). This is deliberately <i>not</i> inferred from
             <paramref name="resultingStatus"/>: <see cref="P:TigerCS.Domain.Modules.Ticketing.IntakeRecord.CrmVerificationStatus"/>
             records a differently-scoped, CRM-named concept (whether a customer
             match was verified) that must not silently double as "a Unit was
             selected" — a customer verified with no Unit chosen must never
             classify as unit-related. An intake already unit-related at creation
             (a caller outside the current wizard, e.g. a future channel that does
             collect this up front) is left exactly as it was — this never
             reclassifies a genuinely unit-related intake as not, regardless of
             this Ticket's own outcome.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.InteractionContextSource">
            <summary>
            Who produced a ticket's interaction context — the explicit
            Genesys-vs-local distinction of the Workflow/Automation architecture.
            Interaction <i>routing</i> is Genesys's concern; Ticketing only records
            the context it was given (or created locally) and never re-derives it.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.InteractionContextSource.Ticketing">
            <summary>Ticketing created the context locally — today the Face-to-Face / walk-in exception, where the agent selects channel, customer phone, and department by hand. Genesys fields are always null.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.InteractionContextSource.Genesys">
            <summary>Genesys determined the interaction context (channel, queue, agent, conversation) and handed it to Ticketing. Requires at least the conversation id; every other Genesys field is optional until the integration contract is finalized.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.PendingKind">
            <summary>
            Why a ticket is pending — the structured distinction the Workflow/SLA
            phase requires on top of the existing <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatus"/> values.
            Values deliberately mirror the two existing pending statuses 1:1 (no new
            status is introduced): <see cref="F:TigerCS.Domain.Modules.Ticketing.PendingKind.Customer"/> accompanies
            <see cref="F:TigerCS.Domain.Modules.Ticketing.TicketStatus.PendingCustomer"/>,
            <see cref="F:TigerCS.Domain.Modules.Ticketing.PendingKind.InternalOrThirdParty"/> accompanies
            <see cref="F:TigerCS.Domain.Modules.Ticketing.TicketStatus.PendingThirdParty"/>. Whether either kind pauses
            the SLA clock remains a pending business decision
            (docs/Workflow-SLA-Configuration-Phase1.md §5) — this enum only records
            the why, never a pause.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.PendingKind.Customer">
            <summary>Waiting on the customer — missing documents, pending payment, awaiting a customer response.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.PendingKind.InternalOrThirdParty">
            <summary>Waiting on an internal department (e.g. Accounting approval, Maintenance) or an external party.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.ResolutionOutcome">
            <summary>MVP-Data-Dictionary.md §2.10/§2.14, Solution-Analysis.md §5.5. Set once, at the Resolve action; one of the five independent lifecycle dimensions (ADR-0008).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.SlaState">
             <summary>
             Tiger-CS-Ticketing-Architecture-Design.md line 325 / Solution-Analysis.md
             §5.4. One of the five independent lifecycle dimensions (ADR-0008).
            
             <para>
             <b>A ticket-level summary of two independent clocks.</b> First Response
             and Resolution breach independently and are tracked independently, each
             with its own due timestamp and its own breach flag on every
             <c>TicketSlaInstance</c> row (ADR-0009). This single column reports the
             ticket's overall position: <see cref="F:TigerCS.Domain.Modules.Ticketing.SlaState.Breached"/> if either clock has
             breached (sticky, mirroring the flags' own immutability rule,
             MVP-ERD.md §2.15), <see cref="F:TigerCS.Domain.Modules.Ticketing.SlaState.Met"/> once the resolution landed within
             its deadline and nothing had breached, <see cref="F:TigerCS.Domain.Modules.Ticketing.SlaState.Running"/> otherwise.
             </para>
            
             <para>
             <b><see cref="F:TigerCS.Domain.Modules.Ticketing.SlaState.Paused"/> has exactly one producer in this pilot</b>, and it
             is not the pause/resume feature: a provisional ticket created during a CRM
             outage carries it because FR-TKT-09 says an unverified ticket "does not
             start its SLA clock", and it becomes <see cref="F:TigerCS.Domain.Modules.Ticketing.SlaState.Running"/> at
             reconciliation. SLA pause/resume proper — <c>TicketSlaPausePeriods</c>,
             the PendingCustomer/PendingThirdParty clock stop — is explicitly not built
             in this pilot (MVP-Implementation-Backlog.md §0.2, a disclosed
             limitation), so no status change pauses a running clock here.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.Ticket">
             <summary>
             MVP-ERD.md §2.10 / MVP-Data-Dictionary.md §2.10 — the ticket aggregate
             root and its five independent lifecycle dimensions (ADR-0008): creation,
             assignment/transfer, the status sub-machine, resolve/close, and — added
             by the SLA and Escalation increment — first-response recording, the
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.EscalationLevel"/> dimension, and the <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.SlaState"/>
             projection of the two independent SLA clocks. Priority change
             (MVP-Implementation-Backlog.md S-14) is still not exposed by any method
             below.
            
             <para>
             <b>UnitReferenceId/ContactReferenceId/RequesterSnapshot are optional —
             a customer match is enrichment, never a Ticket creation gate.</b>
             Customer lookup (CRM/PACT/Tasleeh, <c>CustomerLookupAppService</c>) is
             enrichment/identification, not a promotion gate: whether the lookup finds
             a match, finds nothing, or a source fails to answer, ticket creation
             proceeds the same way (<see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateUnverified(System.String,System.Int32,System.Int32,System.Byte,System.String,System.DateTime,System.String,System.String)"/>). Only when the
             caller already has a resolved local unit/contact pair — because the agent
             selected a lookup match — is <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)"/> used instead,
             and both fields are populated together, never one without the other. A
             ticket that started unverified is not stuck that way: it may later be
             enriched or linked once customer/unit information becomes available,
             via <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.ReconcileVerification(System.Int32,System.Int32)"/>.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.RequestTypeId">
            <summary>
            Workflow/SLA Configuration phase 2 — which configured
            <c>RequestType</c> this ticket follows, resolving its workflow
            template, capabilities, assignment rule, and request-type SLA layer.
            Nullable on purpose: tickets created before this phase (and callers
            that don't yet classify) carry null and keep the exact pre-phase-2
            behavior — workflow capabilities are only enforced when a request
            type is present, never guessed. Set once at creation via
            <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.ClassifyRequestType(System.Int32)"/>; validation that it belongs to the
            ticket's department is the creating application service's job.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.CrmBuyerCustomerId">
            <summary>
            Business-rule change: the real CRM Buyer Lookup match the agent
            selected (<c>GET /api/crm/buyers</c> — phone search only, never a
            Unit/Project search). A different identifier space from
            <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.UnitReferenceId"/>/<see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ContactReferenceId"/> (that
            pair is the older CRM-unit-number cache, ICrmGateway — see that
            interface's remarks); the four CRM Buyer ids are always set together
            or not at all, mirroring the Unit/Contact pair's own invariant. Set
            only when the agent explicitly selected one of the matched Buyer's
            eligible (Sold/Contract) units — CRM never auto-selects one.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.CrmBuyerCustomerName">
            <summary>Immutable, ticket-time snapshot of the selected CRM Buyer match's display text — never re-read from CRM afterward, same ADR-0007 reasoning as <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketRequesterSnapshot"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.ManualProjectName">
            <summary>
            Business-rule change: when CRM Buyer Lookup found no match (or CRM was
            unavailable), the agent manually enters Project and Unit Number
            instead — both required together, and never used to run another CRM
            lookup (CRM is searched by phone number only). Mutually exclusive with
            the CrmBuyer* fields above: a ticket carries one or the other, never
            both.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.CustomerVerificationSource">
            <summary>
            Which external customer-lookup source verified the customer identity
            this ticket was created against, when it was not the real CRM Buyer
            Lookup — e.g. "Pact" (a <c>CustomerLookupSource</c> name, stored as a
            string so future sources need no schema change). A matched PACT/
            Tasleeh customer IS verified — against that source — even though no
            local UnitReference/ContactReference exists for it; what stays
            CRM-scoped is <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.VerificationStatus"/> (see
            <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateFromExternalLookup(System.String,System.Int32,System.String,System.String,System.String,System.String,System.String,System.Int32,System.Byte,System.String,System.DateTime)"/>'s remarks). Null for CRM Buyer
            tickets (their source is the CrmBuyer* fields themselves) and for
            plain manual entry.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.ExternalCustomerId">
            <summary>
            The source's own identifier for the verified customer (for PACT, its
            tenantID) — an external identifier only, stored for auditability,
            reconciliation, and finding this customer's other tickets; never a
            foreign key, and never resolved through a local cache table (none
            exists for these sources). Travels with
            <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.CustomerVerificationSource"/>, never alone.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.ExternalUnitId">
            <summary>The source's own identifier for the selected unit (for PACT, its unitID) — same external-identifier-only discipline as <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ExternalCustomerId"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.Ticket.RowVersion">
            <summary>
            MVP-Data-Dictionary.md §2.10 — optimistic concurrency token. Deferred
            by the ticket-creation increment ("no endpoint in this increment
            mutates an existing Ticket") to the increment that adds assignment/
            transfer/status-change — this one. EF Core maps this as SQL Server
            `rowversion`; never set from application code.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)">
            <summary>The customer-match path: the caller already has a resolved local unit/contact pair (the agent selected a CRM lookup match). Fully verified from the moment it exists.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateUnverified(System.String,System.Int32,System.Int32,System.Byte,System.String,System.DateTime,System.String,System.String)">
            <summary>
            The default path whenever no resolved unit/contact pair is available
            at creation time — whether the intake is not unit-related, the
            customer lookup found no match, a lookup source failed to answer, or
            the agent simply proceeded without selecting one. Customer lookup is
            enrichment, never a Ticket creation gate (see this type's remarks), so
            none of those reasons changes this factory's behavior: no Unit/Contact
            reference and no requester snapshot exist yet, and the SLA clock still
            starts immediately — nothing here is pending on an external system.
            <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.VerificationStatus"/> stays <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Unverified"/>
            until (and unless) <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.ReconcileVerification(System.Int32,System.Int32)"/> later links a
            unit/contact once one becomes available.
            </summary>
            <param name="ticketNumber">The generated, unique ticket number.</param>
            <param name="departmentId">The originating (and initial current) department.</param>
            <param name="categoryId">The selected, active Ticket Category.</param>
            <param name="priorityId">1=Critical, 2=High, 3=Medium, 4=Low.</param>
            <param name="requestSummary">The caller's request, in the agent's words.</param>
            <param name="createdAtUtc">Creation time, in UTC — also the SLA clock-start event.</param>
            <param name="manualProjectName">
            Business-rule change: required together with <paramref name="manualUnitNumber"/>
            when the CRM Buyer Lookup found no match for the intake's phone number
            (or CRM was unavailable) — the caller enforces the pairing before this
            factory runs (<c>TicketCreationAppService.CreateAsync</c>), same
            division of responsibility as <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerifiedFromCrmBuyer(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Int32,System.String,System.String,System.String,System.Int32,System.Byte,System.String,System.DateTime)"/>.
            </param>
            <param name="manualUnitNumber">See <paramref name="manualProjectName"/>.</param>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerifiedFromCrmBuyer(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Int32,System.String,System.String,System.String,System.Int32,System.Byte,System.String,System.DateTime)">
            <summary>
            The real CRM Buyer Lookup match path: the agent explicitly selected
            one of a matched Buyer's eligible (Sold/Contract) units from
            <c>GET /api/crm/buyers</c>'s results. A different identifier space
            from <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)"/>'s Unit/Contact reference pair (that
            factory is the older CRM-unit-number cache path, ICrmGateway) — this
            one never touches <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.UnitReferenceId"/>/<see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ContactReferenceId"/>.
            Fully verified from the moment it exists, same as <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)"/>:
            a real CRM Buyer match is exactly the kind of confirmed customer
            identification <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Verified"/> means.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateFromExternalLookup(System.String,System.Int32,System.String,System.String,System.String,System.String,System.String,System.Int32,System.Byte,System.String,System.DateTime)">
             <summary>
             The external-lookup path: the agent explicitly selected one customer's
             one unit from a matched PACT/Tasleeh result in the department-aware
             customer lookup. The customer identity IS verified — against that
             external source — and this factory records exactly which source and
             which of its own customer/unit identifiers
             (<see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.CustomerVerificationSource"/>/<see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ExternalCustomerId"/>/
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ExternalUnitId"/> — external identifiers only, never
             foreign keys and never a local cache row), alongside the same
             human-readable Project/Unit snapshot the manual path stores.
            
             <para>
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.VerificationStatus"/> deliberately stays
             <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Unverified"/>: that enum is the
             CRM-named concept the rest of the system keys on (reconciliation
             still may link a local CRM Unit/Contact reference later; a PACT match
             carries no such pair), so external verification is expressed by the
             source fields — display and reporting derive "Verified via PACT" from
             them — rather than by widening a CRM-scoped status.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.ReconcileVerification(System.Int32,System.Int32)">
             <summary>
             Links a unit/contact pair onto a ticket that did not have one at
             creation — the "later enrichment" story for a ticket that started
             <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Unverified"/> (business-rule change:
             customer lookup no longer gates creation, so this is the normal way a
             ticket picks up a customer match afterward, not a rare recovery path).
            
             <para>
             <b>Deliberately a bare state transition, not full orchestration —
             same division of responsibility as <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)"/>.</b>
             This method does not itself create a <c>TicketRequesterSnapshot</c>;
             sourcing a genuinely resolved <c>unitReferenceId</c>/
             <c>contactReferenceId</c> pair and constructing the resulting
             <c>TicketRequesterSnapshot</c> is the calling application service's
             job, done alongside this call in one transaction — see
             <c>TicketCreationAppService.CreateAsync</c> for the pattern this
             method's caller mirrors. A caller that invokes this with unvalidated
             IDs, or that skips writing the snapshot, produces a
             <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Verified"/> ticket that violates
             ADR-0007 — that correctness is this method's caller's responsibility,
             not something its own signature enforces.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.ClassifyRequestType(System.Int32)">
            <summary>
            Records which configured RequestType this ticket follows — write-once,
            at creation, by the creating application service (the same bare
            state-transition division of responsibility as
            <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.ReconcileVerification(System.Int32,System.Int32)"/>: department/active validation
            happens before this is called).
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.AssignTo(System.Guid)">
            <summary>MVP-API-Contracts.md §3.5 / §2.12 — sets the current owner and appends a superseding <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketAssignment"/> row is the caller's job (this method only updates the ticket's own denormalized pointer).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.TransferToDepartment(System.Int32)">
            <summary>MVP-API-Contracts.md §3.6 — moves CurrentDepartmentId, leaves OriginatingDepartmentId untouched (write-once, MVP-ERD.md §2.3), and clears the current owner: the receiving department must explicitly claim/assign it.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.ChangeStatus(TigerCS.Domain.Modules.Ticketing.TicketStatus)">
            <summary>
            Solution-Analysis.md §5.6's TicketStatus transition table, restricted
            to the "work" sub-machine this method owns: Open→InProgress
            (requires an already-assigned owner) and InProgress↔PendingCustomer/
            PendingThirdParty (pivoting through InProgress, not directly between
            the two Pending states). Resolved/Closed are reached only via
            <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.Resolve(TigerCS.Domain.Modules.Ticketing.ResolutionOutcome,System.Nullable{System.Int64})"/>/<see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.Close"/> — deliberately distinct
            operations, not reachable through this method (per this increment's
            scope: Resolve/Close stay separate actions).
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.Resolve(TigerCS.Domain.Modules.Ticketing.ResolutionOutcome,System.Nullable{System.Int64})">
            <summary>
            MVP-API-Contracts.md §3.9 / Solution-Analysis.md §5.6 — marks the
            underlying work done. Valid only from InProgress/PendingCustomer/
            PendingThirdParty (never directly from Open, never twice). Does not
            itself create the <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketResolution"/> row or write audit/
            history — mirrors <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.CreateVerified(System.String,System.Int32,System.Int32,System.Int32,System.Int32,System.Byte,System.String,System.DateTime)"/>'s "bare state
            transition" division of responsibility; the calling application
            service does both in the same transaction.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.Close">
            <summary>MVP-API-Contracts.md §3.10 — the final, CS-layer-only close, distinct from Resolve. Requires a current TicketResolutions row (enforced by the caller — this method only enforces the TicketStatus precondition).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.Reopen">
             <summary>
             FR-RES-04 / MVP-API-Contracts.md §3.11 — Reopen is a domain event,
             not a status value: a Resolved or Closed ticket returns to
             InProgress, <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ReopenCount"/> increments, and the live
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ResolutionOutcome"/>/<see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.DuplicateOfTicketId"/>
             return to unset (Solution-Analysis.md §5's "the field returns to
             unset until the ticket is resolved/closed again"). The prior outcome
             is preserved as history on its TicketResolutions row — flipping that
             row's IsCurrent, writing status history and audit are the calling
             application service's job, done in the same transaction (same bare
             state-transition division of responsibility as <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.Resolve(TigerCS.Domain.Modules.Ticketing.ResolutionOutcome,System.Nullable{System.Int64})"/>/
             <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.Close"/>).
            
             <para>
             Deliberately <i>not</i> gated on <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.EnsureNotClosed"/>:
             Reopen is the single sanctioned exit from closed-ticket immutability
             (System-Architecture.md's <c>Closed → InProgress: Reopen (within
             window)</c> transition). The reopen window itself (ISSUE-011 — 7
             days, configurable) is a business-rule check owned by the
             application service; this method carries no clock.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.RecordAcknowledgementSent(System.DateTime)">
             <summary>
             Records that the <b>automated</b> acknowledgement (FR-NOT-01) was
             delivered.
            
             <para>
             <b>This is not, and can never become, a First Response event.</b>
             FR-SLA-05/ISSUE-019 exist precisely because an acknowledgement fires
             within seconds of creation and would otherwise "satisfy" every
             first-response target automatically, making the KPI meaningless. This
             method therefore writes <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.AcknowledgementSentAtUtc"/> and
             touches nothing else — not <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.FirstHumanResponseAtUtc"/>, not
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.SlaState"/>, not <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.TicketStatus"/>. The two
             timestamps are separate columns (MVP-Data-Dictionary.md §2.10) read by
             separate code paths, and <c>SlaBreachProcessor</c> resolves First
             Response from <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.FirstHumanResponseAtUtc"/> alone.
             </para>
            
             <para>
             Write-once, and deliberately <i>not</i> gated on
             <see cref="M:TigerCS.Domain.Modules.Ticketing.Ticket.EnsureNotClosed"/>. Write-once is the delivery-side
             duplicate guard: a redelivered Outbox message reaching an
             already-acknowledged ticket is refused here rather than emailing the
             customer twice. The closed-ticket gate is omitted because an
             acknowledgement is an asynchronous consequence of creation, and a
             ticket created and closed within the dispatcher's polling interval
             must still be able to record what was already sent — refusing would
             lose the record of a delivery that genuinely happened, which is worse
             than recording it.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.RecordFirstHumanResponse(System.DateTime)">
             <summary>
             ISSUE-019 / MVP-API-Contracts.md §5.2 — records the first genuine,
             human-authored response to the customer. Write-once at the ticket
             level (MVP-ERD.md §2.10): a second call throws rather than
             overwriting, because the First Response SLA is measured against the
             <i>first</i> engagement and a later correction would silently move a
             contractual measurement.
            
             <para>
             The automated acknowledgement never reaches this method, on any
             channel — that is <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.AcknowledgementSentAtUtc"/>'s separate
             job, and keeping the two apart is the whole point of ISSUE-019.
             </para>
            
             <para>
             <paramref name="occurredAtUtc"/> may precede
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.CreatedAtUtc"/>: SLA-Architecture.md §16's Example E has a
             Genesys call answered at 08:58 satisfying a ticket created moments
             later at 09:00, and the actual moment of human engagement is the
             correct value there. No Genesys adapter ships in this increment; the
             method simply does not forbid the case its own approved example
             requires.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.RaiseEscalationLevel(TigerCS.Domain.Modules.Ticketing.EscalationLevel)">
             <summary>
             Advances the <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.EscalationLevel"/> dimension — and nothing
             else.
            
             <para>
             <b>Escalation is a separate dimension from <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.TicketStatus"/></b>
             (ADR-0008, Solution-Analysis.md §5.3/§7.6: "escalating a ticket never
             removes it from active work"). This method deliberately does not
             touch <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.TicketStatus"/>, <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.ResolutionOutcome"/> or
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.SlaState"/>, so an escalated ticket stays exactly as Open
             or InProgress as it was.
             </para>
            
             <para>
             The level only ever rises. ADR-0011 models it as a state rather than
             a counter, so an automatic Level 2 raised by a later breach must not
             pull a ticket back down from a Level 4 a CS Manager already set.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.MarkSlaBreached">
             <summary>
             Projects a recorded SLA breach onto the ticket's own
             <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.SlaState"/> dimension (SLA-Architecture.md §11).
            
             <para>
             The two clocks breach independently and are tracked independently on
             <c>TicketSlaInstances</c> (ADR-0009); <see cref="P:TigerCS.Domain.Modules.Ticketing.Ticket.SlaState"/> is a
             single column, so it carries the ticket-level summary: breached if
             <i>either</i> clock is. Sticky, matching the breach flags' own
             immutability rule (MVP-ERD.md §2.15) — nothing moves this back to
             Running or Met.
             </para>
             </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.MarkSlaMet">
            <summary>
            Marks the SLA met once the resolution landed within its deadline.
            A breach already recorded wins and is never downgraded to Met, so a
            ticket that missed its First Response target and then resolved on
            time still reports Breached.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.Ticket.EnsureNotClosed">
            <summary>Closed-ticket immutability (PR correction): every mutating method above calls this first — a Closed ticket accepts no further Assign/Transfer/ChangeStatus/Resolve/Close.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.ApprovalStatus">
            <summary>How one approval cycle stands. A small dedicated state model — deliberately NOT <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatus"/> values: approval state and ticket lifecycle are different concerns and stay in different records.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketApproval">
             <summary>
             One approval cycle of a ticket (Workflow/Automation phase 3) — the
             authoritative approval state, independent of <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatus"/>.
             A ticket waiting on Accounting shows lifecycle
             <see cref="F:TigerCS.Domain.Modules.Ticketing.TicketStatus.PendingThirdParty"/> (with its structured pending
             reason) while THIS record is what actually says
             "AccountingApproval: Pending".
            
             <para>
             <b>History is never overwritten.</b> A decision is write-once; a later
             re-request (e.g. after a rejection) opens a NEW cycle and flips this
             row's <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketApproval.IsCurrent"/> — the same append-plus-supersede pattern as
             <c>TicketAssignment</c>/<c>TicketResolution</c>. At most one Pending
             cycle per (ticket, approval type) exists at a time, guaranteed by a
             filtered unique index.
             </para>
            
             <para>
             The target columns are a snapshot of the requirement's target at request
             time, so history stays truthful even if the configuration is later
             re-pointed (e.g. Accounting moving from provisional department to an
             approval role).
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketApproval.DecisionAtUtc">
            <summary>The decision moment — for an approved <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.AccountingApproval"/>/<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.CustomerServiceApproval"/> this is the timestamp phase 4's ApprovalReceived/CustomerServiceApproved SLA triggers read (via the typed workflow event written alongside).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketApproval.DecisionComment">
            <summary>Required on rejection; optional on approval/cancellation.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketApproval.IsCurrent">
            <summary>True while this row is the ticket's current cycle of its type; flipped by a superseding re-request, never deleted.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketApproval.CorrelationId">
            <summary>Shared with the workflow event, status history, and audit rows written in the same operation.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketApproval.Request(System.Int64,TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement,System.Guid,System.DateTime,System.String,System.Guid)">
            <summary>Opens a new Pending cycle from a configured requirement's target. Preventing a duplicate active cycle of the same type is the calling service's pre-check plus the filtered unique index.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketApproval.Reject(System.Guid,System.DateTime,System.String)">
            <summary>A rejection always carries its why — the reason is mandatory, and what happens operationally afterward stays an explicit, separate action (never an automatic resolve/close).</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketApproval.Cancel(System.Guid,System.DateTime,System.String)">
            <summary>Withdraws a still-pending request (e.g. raised in error). The record stays as history like every other outcome.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketApproval.MarkSuperseded">
            <summary>Flipped when a later cycle of the same type supersedes this one — append-only, this row is never deleted.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.ApprovalAlreadyDecidedException">
            <summary>Approval decisions are write-once — a second decision on the same cycle is a defect, not a state change.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.ApprovalAlreadyDecidedException.#ctor(System.Int64,TigerCS.Domain.Modules.Ticketing.ApprovalStatus)">
            <summary>Approval decisions are write-once — a second decision on the same cycle is a defect, not a state change.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketAssignment">
            <summary>
            MVP-ERD.md §2.12 / MVP-Data-Dictionary.md §2.12 — append-only assignment
            history. A reassignment inserts a new row and flips the prior current
            row's <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketAssignment.IsCurrent"/> to false; it never updates the prior row's
            <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketAssignment.AssignedEmployeeId"/>.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketAssignment.MarkSuperseded">
            <summary>Flipped when a later assignment supersedes this one — append-only, this row is never deleted (MVP-ERD.md §2.12).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketAlreadyVerifiedException">
            <summary>Ticket.ReconcileVerification links a unit/contact pair onto a ticket that does not have one yet — a ticket already Verified has nothing left to reconcile.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketAlreadyVerifiedException.#ctor(System.Int64)">
            <summary>Ticket.ReconcileVerification links a unit/contact pair onto a ticket that does not have one yet — a ticket already Verified has nothing left to reconcile.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.InvalidTicketStatusTransitionException">
            <summary>Solution-Analysis.md §5.6 transition table / ADR-0008: the requested TicketStatus is not reachable from the ticket's current TicketStatus.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.InvalidTicketStatusTransitionException.#ctor(System.Int64,TigerCS.Domain.Modules.Ticketing.TicketStatus,TigerCS.Domain.Modules.Ticketing.TicketStatus)">
            <summary>Solution-Analysis.md §5.6 transition table / ADR-0008: the requested TicketStatus is not reachable from the ticket's current TicketStatus.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketNotAssignedException">
            <summary>MVP-API-Contracts.md §3.7's "must go through Open→InProgress with an assigned owner" requirement — a ticket cannot start being worked before it has an owner.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketNotAssignedException.#ctor(System.Int64)">
            <summary>MVP-API-Contracts.md §3.7's "must go through Open→InProgress with an assigned owner" requirement — a ticket cannot start being worked before it has an owner.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketNotEligibleForResolutionException">
            <summary>Solution-Analysis.md §5.6: Resolve is only valid from InProgress/PendingCustomer/PendingThirdParty, never directly from Open or a terminal status.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketNotEligibleForResolutionException.#ctor(System.Int64,TigerCS.Domain.Modules.Ticketing.TicketStatus)">
            <summary>Solution-Analysis.md §5.6: Resolve is only valid from InProgress/PendingCustomer/PendingThirdParty, never directly from Open or a terminal status.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketNotYetResolvedException">
            <summary>MVP-API-Contracts.md §3.10: `409 not-yet-resolved` — Close requires a current TicketResolutions row.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketNotYetResolvedException.#ctor(System.Int64)">
            <summary>MVP-API-Contracts.md §3.10: `409 not-yet-resolved` — Close requires a current TicketResolutions row.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.DuplicateChainNotAllowedException">
            <summary>MVP-ERD.md §2.10: DuplicateOfTicketId must reference a genuine, non-duplicate original — no chains of duplicates pointing to duplicates.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.DuplicateChainNotAllowedException.#ctor(System.Int64,System.Int64)">
            <summary>MVP-ERD.md §2.10: DuplicateOfTicketId must reference a genuine, non-duplicate original — no chains of duplicates pointing to duplicates.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketAlreadyInTargetDepartmentException">
            <summary>MVP-ERD.md §2.3's write-once OriginatingDepartmentId / §3.6: a transfer to the ticket's own current department is not a valid transfer.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketAlreadyInTargetDepartmentException.#ctor(System.Int64,System.Int32)">
            <summary>MVP-ERD.md §2.3's write-once OriginatingDepartmentId / §3.6: a transfer to the ticket's own current department is not a valid transfer.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketClosedException">
            <summary>
            Closed-ticket immutability (PR correction): a Closed ticket accepts no
            further mutation of any kind — Assign, Transfer, ChangeStatus, Resolve,
            and Close-again all reject with this exception before any state changes
            or writes occur. Reading a Closed ticket's detail, history, and notes
            remains unaffected — this exception is only ever thrown from a mutating
            domain method.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketClosedException.#ctor(System.Int64)">
            <summary>
            Closed-ticket immutability (PR correction): a Closed ticket accepts no
            further mutation of any kind — Assign, Transfer, ChangeStatus, Resolve,
            and Close-again all reject with this exception before any state changes
            or writes occur. Reading a Closed ticket's detail, history, and notes
            remains unaffected — this exception is only ever thrown from a mutating
            domain method.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketNotEligibleForReopenException">
            <summary>FR-RES-04 / MVP-API-Contracts.md §3.11: Reopen is only valid from Resolved or Closed — an actively-worked ticket has nothing to reopen.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketNotEligibleForReopenException.#ctor(System.Int64,TigerCS.Domain.Modules.Ticketing.TicketStatus)">
            <summary>FR-RES-04 / MVP-API-Contracts.md §3.11: Reopen is only valid from Resolved or Closed — an actively-worked ticket has nothing to reopen.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.AcknowledgementAlreadySentException">
            <summary>
            <c>Ticket.AcknowledgementSentAtUtc</c> is write-once (MVP-Data-Dictionary.md
            §2.10). Thrown when a redelivered <c>TicketCreated</c> Outbox message
            reaches a ticket whose automated acknowledgement was already delivered —
            the domain-level half of "duplicate processing does not send twice"
            (ADR-0013/ADR-0014). Callers treat it as "already done", not as an error.
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.AcknowledgementAlreadySentException.#ctor(System.Int64,System.DateTime)">
            <summary>
            <c>Ticket.AcknowledgementSentAtUtc</c> is write-once (MVP-Data-Dictionary.md
            §2.10). Thrown when a redelivered <c>TicketCreated</c> Outbox message
            reaches a ticket whose automated acknowledgement was already delivered —
            the domain-level half of "duplicate processing does not send twice"
            (ADR-0013/ADR-0014). Callers treat it as "already done", not as an error.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketRequestTypeAlreadySetException">
            <summary>Workflow/SLA Configuration phase 2: a ticket's request type is write-once — re-classification is not a supported operation in this phase.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketRequestTypeAlreadySetException.#ctor(System.Int64,System.Int32)">
            <summary>Workflow/SLA Configuration phase 2: a ticket's request type is write-once — re-classification is not a supported operation in this phase.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketInteraction">
             <summary>
             One customer interaction associated with a ticket — a ticket accumulates
             <b>many</b> of these over its lifetime (the original inbound Genesys
             call, a follow-up inbound call, an outbound call, another Genesys
             conversation, a future WhatsApp exchange, a Face-to-Face follow-up), each
             independently retaining its source, channel, customer phone, and — where
             Genesys handled it — the Genesys context, verbatim. Persisted for audit,
             history, reporting, and Ticket ↔ Genesys conversation traceability. This
             is a <b>record of what Genesys (or the agent) said</b>, never routing
             logic: Ticketing does not reproduce Called Number → Queue mapping, and
             none of this data drives department routing inside Ticketing
             (Category/Request Type do that, exactly as before).
            
             <para>
             <b>Exactly one interaction per ticket is the originating one</b>
             (<see cref="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.IsOriginatingInteraction"/>) — the interaction the ticket
             was created from, written by ticket creation in the same transaction. A
             filtered unique index enforces at-most-one at the database. Later
             interactions (recorded by future phases) are appended with the flag
             false and never touch the originating row.
             </para>
            
             <para>
             <b>Every Genesys field is nullable by design.</b> The exact Genesys API
             contract is not finalized; the fields below are the context Genesys is
             expected to provide (channel, customer phone, called/destination number,
             queue id/name, agent id/name, conversation id, interaction start,
             direction). Face-to-Face / walk-in interactions never have any of them —
             <see cref="F:TigerCS.Domain.Modules.Ticketing.InteractionContextSource.Ticketing"/> rows enforce that they
             stay null. Only the conversation id is required for a
             <see cref="F:TigerCS.Domain.Modules.Ticketing.InteractionContextSource.Genesys"/> row, because without it
             the row cannot link back to the interaction at all.
             </para>
            
             <para>
             <b>CustomerPhone here is the customer's identity input</b> (the number
             CRM/PACT/Tasleeh verification searches with), distinct from
             <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CalledNumber"/> — the Tiger number the customer dialed,
             meaningful only on the Genesys side. Genesys identifiers are external
             identifiers stored as strings, never foreign keys, and are for
             audit/support/integration surfaces — not for prominent display in the
             main CS UI.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.IsOriginatingInteraction">
            <summary>True on the one interaction the ticket was created from — at most one per ticket (filtered unique index). Set at construction, never mutated.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CustomerPhone">
            <summary>The customer's phone number for this interaction — the identity input for customer verification, preserved per interaction for reporting.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CalledNumber">
            <summary>The company/destination number the interaction arrived on (Genesys side), or null — never used by Ticketing for routing.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.InteractionStartedAtUtc">
            <summary>When the interaction started on the Genesys side, where provided — may precede ticket creation.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.Direction">
            <summary>Interaction direction as reported by Genesys (e.g. "Inbound"), where available. Free text until the integration contract fixes an enumeration.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CreatedAtUtc">
            <summary>When this row was recorded by Ticketing (creation/audit timestamp), as distinct from <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketInteraction.InteractionStartedAtUtc"/> — Genesys' own clock.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CreateLocal(System.Int64,TigerCS.Domain.Modules.Ticketing.Channel,System.String,System.DateTime,System.Boolean)">
            <summary>A Face-to-Face / locally-created interaction: channel and phone the agent entered; every Genesys field stays null, by construction.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketInteraction.CreateFromGenesys(System.Int64,TigerCS.Domain.Modules.Ticketing.Channel,System.String,System.String,System.String,System.String,System.String,System.String,System.String,System.Nullable{System.DateTime},System.String,System.DateTime,System.Boolean)">
            <summary>
            A Genesys-provided interaction. Only the conversation id is mandatory —
            every other field is optional until the Genesys API contract is
            finalized, and absent values are stored as null rather than guessed.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketNote">
            <summary>
            MVP-ERD.md §2.18 / MVP-Data-Dictionary.md §2.18 — an internal note.
            Immutable once written; no update/delete path exists at any privilege
            level (a correction is posted as a new note). Customer-facing comments
            are out of scope for this increment.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord">
             <summary>
             One structured pending period of a ticket (Workflow/SLA Configuration
             phase 2): why the ticket was put on Pending, by whom, from which status,
             and — once resumed — when and by whom it resumed. Append-only alongside
             the existing <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatusHistory"/> rows (which keep recording
             the raw status transitions); this table adds the structured reason and
             the pause window the phase-4 SLA calculation will read <b>if</b> the
             business approves pausing — recording the window is deliberately separate
             from deciding whether it pauses anything.
            
             <para>
             At most one open record (<see cref="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.ResumedAtUtc"/> null) exists per
             ticket, because the status machine allows only one Pending state at a
             time; the writing application service enforces it transactionally.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.Reason">
            <summary>Required — a ticket is never pending without a recorded why (e.g. "Missing passport copy", "Awaiting Accounting approval").</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.PreviousStatus">
            <summary>The status the ticket held before entering Pending — with the current machine always <see cref="F:TigerCS.Domain.Modules.Ticketing.TicketStatus.InProgress"/>, stored explicitly so history never has to infer it.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.ResumedAtUtc">
            <summary>Null while the ticket is still pending. Set exactly once by <see cref="M:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.Resume(System.Guid,System.DateTime)"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.CorrelationId">
            <summary>Shared with the status-history and audit rows written in the same transaction, so one pending action reads as one event.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.Resume(System.Guid,System.DateTime)">
            <summary>Closes this pending period — write-once; resuming an already-resumed record is a defect.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketPendingRecord.PausedDuration">
            <summary>The paused duration once resumed — raw data for the phase-4 pause calculation, which only applies it where configuration says pending pauses the clock.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketRequesterSnapshot">
             <summary>
             MVP-ERD.md §2.8 / MVP-Data-Dictionary.md §2.8 (ADR-0007) — the immutable,
             write-once record of exactly what the agent read back and the requester
             confirmed, copied from a consumed VerificationSession's own snapshot
             fields (Finding DR-01) — never a fresh CRM/cache read, and never updated
             after insert (no method on this class mutates any field once
             constructed; a future contributor proposing one is a defect, per this
             entity's own ERD note).
            
             <para>
             <b>Relationship to Ticket — 0/1:1, not the mandatory 1:1 the ERD
             describes, for a provisional ticket only.</b> See Ticket's own remarks
             for why: a provisional (ISSUE-006) ticket has no requester to snapshot
             yet. A <see cref="F:TigerCS.Domain.Modules.Ticketing.CrmVerificationStatus.Verified"/> ticket — whether
             verified from the moment it was created, or reconciled later — always
             has exactly one of these rows, matching the ERD's rule for that case
             unchanged.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketResolution">
            <summary>
            MVP-ERD.md §2.14 / MVP-Data-Dictionary.md §2.14 — one row per resolve
            cycle (BR-011: <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketResolution.ResolutionNote"/> is mandatory regardless of
            outcome). On Reopen (FR-RES-04), the current row's <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketResolution.IsCurrent"/>
            flips to false via <see cref="M:TigerCS.Domain.Modules.Ticketing.TicketResolution.Archive"/> — history preserved, never
            deleted — and a new row is created on the next resolution, never
            overwriting the old one (MVP-ERD.md §2.14's Reopen-driven history model).
            </summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.Ticketing.TicketResolution.Archive">
            <summary>
            Reopen (FR-RES-04): this resolution is no longer the ticket's live
            outcome — it becomes history, never deleted. One-way by design; the
            next resolve cycle creates a new row rather than reviving this one.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketStatus">
            <summary>Tiger-CS-Ticketing-Architecture-Design.md line 322. One of the five independent lifecycle dimensions (ADR-0008).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketStatusDimension">
            <summary>MVP-Data-Dictionary.md §2.13 — which of the five independent dimensions (ADR-0008) a <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatusHistory"/> row describes.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketStatusHistory">
             <summary>
             MVP-ERD.md §2.13 / MVP-Data-Dictionary.md §2.13 (ADR-0018) — append-only;
             no update or delete path exists at any privilege level. One row per
             change to any of the five independent dimensions (ADR-0008); ticket
             creation seeds one row per dimension with <c>OldValue = null</c>
             (MVP-API-Contracts.md §3.1's audit note).
            
             <para>
             Written directly by the Ticketing application service in the same
             transaction as the ticket it describes, not via the event-subscriber
             path Module-Design.md's "ownership note" describes — that path requires
             the Outbox/event infrastructure (MVP-Implementation-Backlog.md S-05),
             which has not been built yet. This mirrors the same pragmatic,
             already-established pattern <c>VerificationSessionAppService</c> uses
             for <c>AuditEntries</c> (a direct <c>IAuditEntryWriter</c> call, not an
             event subscriber) ahead of S-05 landing.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.WorkflowEventType">
            <summary>
            The typed business events a ticket's workflow can emit (Workflow/
            Automation phase 3). These are the machine-queryable trigger source the
            phase-4 SLA calculation reads — never parsed out of human audit text.
            Values map onto the phase-1 <c>SlaTriggerType</c> concepts where a
            trigger exists; the maintenance trio additionally carries the Handover
            dependency state.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.ApprovalRequested">
            <summary>An approval cycle was opened.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.ApprovalReceived">
            <summary>A required approval was granted — the semantic event behind the <c>SlaTriggerType.ApprovalReceived</c> trigger (Collections / Send Receipts: the 1 day runs from here, computed in phase 4).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.ApprovalRejected">
            <summary>A required approval was rejected. What happens next stays an explicit operational action.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.CustomerServiceApproved">
            <summary>Customer Service approved — the semantic event behind <c>SlaTriggerType.CustomerServiceApproved</c> (Handover: 1–4 days run from here, computed in phase 4).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.PrerequisitesCompleted">
            <summary>Prerequisites are satisfied — the semantic event behind <c>SlaTriggerType.PrerequisitesCompleted</c> (Registration / Register Unit). Recorded explicitly by an authorized actor, never inferred.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.MaintenanceRequired">
            <summary>Handover: maintenance exists and the process waits on it. No duration is attached — the source defines none.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.MaintenanceNotRequired">
            <summary>Handover: no maintenance dependency — after the NOC the customer proceeds to the tower/key handover.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.Ticketing.WorkflowEventType.MaintenanceCompleted">
            <summary>Handover: the maintenance dependency completed. Not customer-caused; how it affects the clock is a phase-4 decision.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent">
            <summary>
            One typed, append-only workflow event of a ticket — the trustworthy
            timestamp store for conditional SLA triggers and dependency state.
            Written in the same transaction (and under the same correlation id) as
            the approval/audit rows describing the same action; never updated, never
            deleted. Distinct from <see cref="T:TigerCS.Domain.Modules.Ticketing.TicketStatusHistory"/> (which records
            the five lifecycle dimensions) and from <c>AuditEntries</c> (the
            human-auditable trail): this table exists precisely so phase 4 reads
            typed events instead of parsing either of those.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.OccurredAtUtc">
            <summary>When the business event happened — the value a phase-4 SLA trigger reads.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.ActorEmployeeId">
            <summary>Null = the system. Approval-linked events carry the deciding/requesting employee.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.TicketApprovalId">
            <summary>The approval cycle this event belongs to, for the four approval event types; null for prerequisite/maintenance events.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.Note">
            <summary>Short human context (e.g. the recorded reason) — display only; consumers key on <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.EventType"/> and <see cref="P:TigerCS.Domain.Modules.Ticketing.TicketWorkflowEvent.OccurredAtUtc"/>, never on this text.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType">
            <summary>
            The controlled set of approval types (Workflow/Automation phase 3) —
            never free text. Only the two the Customer Service SLA document actually
            supports exist: Collections' Send Receipts depends on Accounting
            approval, and Handover's post-approval stage begins at Customer Service
            approval. No further types are invented; new ones are additive enum
            values with their own <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement"/>
            configuration.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.AccountingApproval">
            <summary>Accounting's approval that gates Collections / Send Receipts ("depends on Accounting Approval; once received, Collections has 1 day").</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.CustomerServiceApproval">
            <summary>Customer Service's approval that gates the Handover process ("Handover approval takes approximately 1–4 days after Customer Service approval").</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind">
            <summary>
            How an approval's authorized approver is expressed — configuration,
            never a hard-coded employee name. Deliberately covers the three shapes
            the business may settle on, so Accounting's still-provisional status
            (full department? approval role? external/internal provider?) can be
            resolved later by re-pointing configuration, not by redesigning the
            model: an external provider would arrive as a new kind, additively.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Department">
            <summary>An active member of the configured department decides (optionally narrowed further by a role name on the requirement).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Role">
            <summary>Any holder of the configured role decides.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Employee">
            <summary>One explicitly configured employee decides.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode">
            <summary>
            How tickets of one request type are assigned inside their department
            (Workflow/Automation phase 2). Deliberately an extensible enum, not a
            hard-coded behavior: future auto-distribution strategies (Round Robin,
            Least Workload, project-based) are additive values with their own
            configuration — nothing in the current shape prevents them, and none of
            them is implemented now.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.DepartmentQueue">
            <summary>
            The default and the universal fallback: the ticket goes to the
            department's queue unassigned, and the department's supervisory level
            assigns the specific employee. This is exactly today's behavior for a
            ticket with no configured rule — never a random employee.
            </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.SpecificEmployee">
            <summary>One configured employee always receives tickets of this request type (e.g. NOC for Resale → its responsible officer).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.Team">
            <summary>
            A configured group works this request type (e.g. AC Issue → the AC
            team). Ownership stays unambiguous: the rule names one primary
            assignee — who becomes the ticket's single current owner, preserving
            the existing one-owner accountability model — plus the further team
            members as configured participants.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings">
             <summary>
             Per-department workflow configuration (Workflow/SLA Configuration
             phase 1) — one optional row per existing <see cref="T:TigerCS.Domain.Modules.IdentityAndAccess.Department"/>; the
             Department entity itself is deliberately untouched. Identity data
             (department id/name/active) stays on Department; employee directory data
             is never duplicated here — the responsible authority is a role name from
             the existing fixed role set, resolved through the existing
             department-membership model at runtime.
            
             <para>
             The three capability flags configure the phase-2 assignment model
             (CS assigns to department → department head assigns to employee) without
             hard-coding it: today's role-set authorization
             (<c>TicketRoleSets</c>) remains the enforcement point, and these flags
             can only narrow behavior per department, mirroring how
             <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType"/> flags narrow a <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate"/>.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.DepartmentId">
            <summary>Also the primary key — at most one settings row per department.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.AllowAssignment">
            <summary>Whether tickets in this department may be assigned to employees at all.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.AllowInternalReassignment">
            <summary>Whether an already-assigned ticket may be reassigned within the department.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.AllowTransferToOtherDepartments">
            <summary>Whether tickets may be transferred out of this department to another.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.HeadRoleName">
            <summary>
            The role that acts as this department's responsible head for
            assignment/escalation purposes — a name from the existing fixed
            <see cref="T:TigerCS.Domain.Modules.IdentityAndAccess.Roles"/> set (default <see cref="F:TigerCS.Domain.Modules.IdentityAndAccess.Roles.DepartmentHead"/>),
            never an employee id: people are resolved through the existing
            role + department-membership model.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.DepartmentWorkflowSettings.SupervisorRoleName">
            <summary>
            The role acting as this department's operational supervisor — the
            level between agent/team and department head in the Agent → Supervisor
            → Head → higher-authority escalation ladder. A name from the existing
            fixed <see cref="T:TigerCS.Domain.Modules.IdentityAndAccess.Roles"/> set; the fixed set has no dedicated
            "Department Supervisor" role, so the provisional default is
            <see cref="F:TigerCS.Domain.Modules.IdentityAndAccess.Roles.DepartmentHead"/> (the department-scoped supervisory
            authority that exists today) until the business decides whether a
            distinct supervisor role is introduced. Supervisor <i>visibility</i>
            stays governed by the existing department-scoped authorization —
            this configuration never widens it.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType">
             <summary>
             The operational configuration unit of the Department → Request Type →
             Workflow → SLA model (Workflow/SLA Configuration phase 1). Each request
             type belongs to exactly one department, selects one reusable
             <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate"/>, and carries the business flags that
             gate lifecycle actions for tickets of that type (enforcement is phase 2).
            
             <para>
             <b>Deliberately distinct from <c>Category</c>.</b> Category remains the
             intake classification/routing taxonomy (every category routes to one
             department, FR-CLS-01); RequestType is the workflow/SLA configuration
             layer. How a ticket acquires its request type (a direct
             <c>Tickets.RequestTypeId</c>, or a Category → RequestType mapping) is a
             phase-2 wiring decision recorded in
             docs/Workflow-SLA-Configuration-Phase1.md — not silently decided here.
             </para>
            
             <para>
             <b>Urgency is priority, not a second request type.</b> "NOC for Resale
             URGENT" is NOT a request type: it is NOC for Resale at the Urgent
             priority, with its own <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy"/> row. The
             Normal/Urgent ↔ Medium/High mapping decision is documented in
             docs/Workflow-SLA-Configuration-Phase1.md.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.Name">
            <summary>Unique within the department (e.g. "Ticketing System" exists under both Customer Service and Collections).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.DefaultPriorityId">
            <summary>The priority a new ticket of this type starts at, from the existing fixed Priorities set — no second priority model.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowAgentPriorityChange">
            <summary>Whether the agent may change the priority away from <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.DefaultPriorityId"/> (e.g. raising a NOC for Resale to Urgent).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowPendingCustomer">
            <summary>Request-type-level gate on Pending Customer — effective only where the template also allows it (<see cref="M:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.Resolve(TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate,TigerCS.Domain.Modules.WorkflowConfiguration.RequestType)"/>).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowPendingInternal">
            <summary>Request-type-level gate on Pending Internal / Third Party — same combination rule as <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowPendingCustomer"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowReopen">
            <summary>
            Whether tickets of this type may be reopened at all. When true, the
            existing <c>ReopenPolicy</c> (window + role authorization) remains the
            final enforcement point — this flag can only remove the capability,
            never widen or bypass that policy.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.RequiredFieldsJson">
            <summary>
            JSON array of intake field keys required for this request type (e.g.
            <c>["UnitNumber","BuyerName"]</c>) — provisional representation until
            the required-fields feature is built; null means no extra requirement.
            Required attachments are deliberately not modeled yet (attachments are
            a later increment).
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement">
             <summary>
             The configured approval requirement of one request type (Workflow/
             Automation phase 3) — configuration, never
             <c>if (RequestTypeName == "Send Receipts")</c> logic. At most one
             requirement per (request type, approval type); the seeded pair mirrors
             the SLA document exactly: Collections / Send Receipts requires
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.AccountingApproval"/>, Handover Request requires
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalType.CustomerServiceApproval"/>.
            
             <para>
             The target (<see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement.TargetKind"/> + the nullable columns) says who
             may decide, resolved through the existing identity model at decision
             time — department membership, role holding, or one configured employee.
             Accounting's provisional status is preserved: today its requirement
             points at the provisionally seeded Accounting department, and moving to
             an approval role or an external provider later is a configuration edit
             (or an additive target kind), never a destructive redesign.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement.TargetDepartmentId">
            <summary>The deciding department for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Department"/>; null otherwise.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement.TargetRoleName">
            <summary>
            For <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Role"/>: the deciding role. For
            <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Department"/>: an optional narrowing —
            the member must also hold this role; null means the provisional
            department-side default applies (see the approval service's
            remarks). Always a name from the fixed <see cref="T:TigerCS.Domain.Modules.IdentityAndAccess.Roles"/> set.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement.TargetEmployeeId">
            <summary>The one configured decider for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.ApprovalTargetKind.Employee"/>; null otherwise.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeApprovalRequirement.BlocksWorkUntilApproved">
            <summary>Whether operational work is expected to wait for the decision (both seeded requirements: true, per the SLA document's sequencing). Informational for the workflow/UI in this phase — SLA consequences are phase 4.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule">
             <summary>
             The configured assignment behavior for one <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType"/>
             (Workflow/Automation phase 2): when a ticket of this type is created with
             its department and request type known, the assignment automation resolves
             this rule instead of asking the CS agent to know every department's
             staff. At most one rule per request type; a request type with no rule (or
             an inactive one) falls back to <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.DepartmentQueue"/>.
            
             <para>
             <b>References existing employees only.</b> <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.PrimaryEmployeeId"/>
             and the team members are ids from the existing Employee/department
             membership model — no employee directory data is duplicated here, and the
             automation re-validates department membership at assignment time (a
             configured employee who left the department degrades safely to the
             department queue rather than producing an invalid assignment).
             </para>
            
             <para>
             <b>Accountability stays single-owner.</b> The existing assignment model
             has one current owner per ticket, and this phase keeps it: a
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.Team"/> rule names the primary assignee (the
             owner) plus further members as configured participants — never several
             ambiguous owners.
             </para>
            
             <para>
             <b>Extensibility (reviewed pre-phase-3, deliberately not implemented):</b>
             today exactly one rule per request type exists (unique index on
             RequestTypeId) and the resolver takes only the request type. If the
             business later needs conditional routing — e.g. by Project or Priority —
             the path is purely additive: nullable condition columns (ProjectId,
             PriorityId, …meaning "matches any" when null) plus a specificity/order
             column on this same table, the unique index relaxed to cover the
             condition columns, and the repository lookup widened to a resolve-by-
             context that picks the most specific active match with the
             unconditional rule as its fallback. No column here changes meaning, no
             data migrates destructively, and <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode"/> values
             stay additive — which is why no speculative condition columns are added
             now.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.PrimaryEmployeeId">
            <summary>The single accountable assignee for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.SpecificEmployee"/>/<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.Team"/>; null for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.DepartmentQueue"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.TeamName">
            <summary>Display label for a team rule (e.g. "AC Team"); configuration text only, never an identity.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.Members">
            <summary>The further team members (excluding the primary) for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.AssignmentMode.Team"/>; empty for the other modes.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.ForDepartmentQueue(System.Int32,System.Boolean)">
            <summary>An explicit department-queue rule — equivalent to having no rule at all, but recordable so the choice is visible configuration rather than an absence.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRule.ForTeam(System.Int32,System.Guid,System.Collections.Generic.IReadOnlyCollection{System.Guid},System.String,System.Boolean)">
            <summary>A team rule: one primary (accountable owner) plus at least one further member.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRuleMember">
            <summary>One further (non-primary) member of a team assignment rule — an existing employee id, never duplicated directory data.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeAssignmentRuleMember.Rule">
            <summary>Navigation back to the owning rule — set by the rule's own factory, never independently.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy">
             <summary>
             The configurable SLA for one (Request Type, Priority) pair — the layer the
             Customer Service SLA document's per-request-type durations live in.
             Complements, and does not replace, the existing per-priority
             <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy"/>: how the two combine (this layer overriding the
             priority default where a row exists, or another precedence rule) is a
             phase-4 calculation decision, not silently decided here.
            
             <para>
             <b>Ranges are stored as the source gives them.</b> "10–12 Days" is
             <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ResolutionTargetValue"/> 10 / <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ResolutionMaximumValue"/>
             12 in <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaDurationUnit.Days"/> — deliberately NOT collapsed to
             one number. Whether the lower bound is officially "target" and the upper
             officially "breach" is a documented-as-pending business interpretation
             (docs/Workflow-SLA-Configuration-Phase1.md); the property names describe
             the stored bounds, not an approved breach rule.
             </para>
            
             <para>
             <b>The pause flags are three-valued on purpose.</b> The SLA document
             confirms customer-caused delays may be placed on Pending, but whether
             Pending pauses the SLA clock is awaiting business approval — <c>null</c>
             means "not yet decided" and phase 4 must treat it as no-pause rather than
             silently choosing; <c>true</c>/<c>false</c> is an explicit configuration.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.PriorityId">
            <summary>The priority tier this row applies to, from the existing fixed set — the Normal/Urgent urgency of the SLA document maps onto these (documented mapping decision), never onto a second priority model.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Trigger">
            <summary>When this SLA's clock starts — see <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType"/>. Send Receipts and Handover deliberately do not start at ticket creation.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Unit">
            <summary>The unit every duration value below is expressed in.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.FirstResponseTargetValue">
            <summary>First Response target, in <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Unit"/>. Null where the source document gives no first-response figure (a documented pending decision — most rows), never a guessed value.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.FirstResponseMaximumValue">
            <summary>Upper bound of a First Response range, in <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Unit"/>. Null when the source gives a single value or none.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ResolutionTargetValue">
            <summary>Resolution lower bound / single value, in <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Unit"/> (e.g. the 10 of "10–12 Days").</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ResolutionMaximumValue">
            <summary>Resolution upper bound, in <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.Unit"/> (e.g. the 12 of "10–12 Days"). Null when the source gives a single value.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.IsImmediate">
            <summary>True for the document's "Immediately" entries (Urgent Ticketing System tickets) — represented as a flag, not as a fabricated zero-duration.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ClockBasis">
            <summary>
            How duration values are counted against a clock. Null = the business
            has not yet decided (business vs. calendar days, weekend/holiday
            treatment — pending decisions 1–3); phase 4 must surface that rather
            than assume. Non-null is an explicit configuration using the existing
            <see cref="T:TigerCS.Domain.Modules.SlaAndEscalation.SlaClockBasis"/> semantics.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.PausesOnPendingCustomer">
            <summary>Whether Pending Customer pauses this SLA's clock. Null = pending business approval — see this type's remarks.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.PausesOnPendingInternal">
            <summary>Whether Pending Internal / Third Party pauses this SLA's clock. Null = pending business approval; internal delays never pause implicitly.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.WarningThresholdPercent">
            <summary>Pre-breach warning threshold in percent of the target elapsed, mirroring <see cref="P:TigerCS.Domain.Modules.SlaAndEscalation.SlaPolicy.WarningThresholdPercent"/>. Null = use the escalation configuration's default (phase 5).</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.SlaDurationUnit">
             <summary>
             The unit a <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy"/>'s duration values are
             expressed in — kept in the source document's own unit (the SLA document
             speaks in days) rather than converted to minutes at seed time, so the
             stored configuration stays recognizably the approved value.
            
             <para>
             <b><see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaDurationUnit.Days"/> is deliberately uninterpreted here.</b> Whether a
             "day" means a calendar day or a business day (and how weekends/UAE public
             holidays count) is an explicitly pending business decision
             (docs/Workflow-SLA-Configuration-Phase1.md); the phase-4 calculation will
             resolve it via <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestTypeSlaPolicy.ClockBasis"/> and the
             existing business-calendar infrastructure once decided.
             </para>
             </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType">
             <summary>
             When a request-type SLA clock starts. Not every SLA starts at ticket
             creation: the Customer Service SLA document states Collections' Send
             Receipts one-day SLA runs from Accounting approval, and Handover's 1–4
             day approval duration runs from Customer Service approval — starting
             those at creation would be wrong, so the trigger is configuration, not an
             assumption.
            
             <para>
             Phase 1 stores the trigger; the clock-start plumbing that honors
             non-creation triggers (approval events, prerequisite completion) arrives
             with phases 3–4. The existing initial-period behavior (clock starts at
             creation, ISSUE-001 Option C) is exactly <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.TicketCreated"/>.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.TicketCreated">
            <summary>The existing default — the clock starts when the ticket is created (ISSUE-001 Option C).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.Assigned">
            <summary>The clock starts when the ticket is assigned.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.ApprovalReceived">
            <summary>The clock starts when the configured approval is received (Collections / Send Receipts: after Accounting approval).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.CustomerServiceApproved">
            <summary>The clock starts when Customer Service approves (Handover: 1–4 days after Customer Service approval).</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.SlaTriggerType.PrerequisitesCompleted">
            <summary>The clock starts once prerequisites are satisfied (Registration / Register Unit: 1–3 days "when everything is OK").</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities">
            <summary>
            The effective lifecycle capabilities for tickets of one request type —
            the template's capability flags combined with the request type's own,
            each gate an AND (a request type can only narrow what its template
            allows, never widen it). This is the single place the combination rule
            lives, so phase-2 transition enforcement and the Ticket Details action
            list can never disagree on it.
            </summary>
            <param name="CanGoPendingCustomer">Whether <c>InProgress → PendingCustomer</c> is available.</param>
            <param name="CanGoPendingInternal">Whether <c>InProgress → PendingThirdParty</c> is available.</param>
            <param name="RequiresApproval">Whether the flow carries an approval stage (phase 3 approval records).</param>
            <param name="CanReopen">Whether Reopen is available at all — when true, the existing <c>ReopenPolicy</c> still decides each actual reopen.</param>
            <param name="CanChangePriority">Whether the agent may change priority away from the request type's default.</param>
        </member>
        <member name="M:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.#ctor(System.Boolean,System.Boolean,System.Boolean,System.Boolean,System.Boolean)">
            <summary>
            The effective lifecycle capabilities for tickets of one request type —
            the template's capability flags combined with the request type's own,
            each gate an AND (a request type can only narrow what its template
            allows, never widen it). This is the single place the combination rule
            lives, so phase-2 transition enforcement and the Ticket Details action
            list can never disagree on it.
            </summary>
            <param name="CanGoPendingCustomer">Whether <c>InProgress → PendingCustomer</c> is available.</param>
            <param name="CanGoPendingInternal">Whether <c>InProgress → PendingThirdParty</c> is available.</param>
            <param name="RequiresApproval">Whether the flow carries an approval stage (phase 3 approval records).</param>
            <param name="CanReopen">Whether Reopen is available at all — when true, the existing <c>ReopenPolicy</c> still decides each actual reopen.</param>
            <param name="CanChangePriority">Whether the agent may change priority away from the request type's default.</param>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.CanGoPendingCustomer">
            <summary>Whether <c>InProgress → PendingCustomer</c> is available.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.CanGoPendingInternal">
            <summary>Whether <c>InProgress → PendingThirdParty</c> is available.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.RequiresApproval">
            <summary>Whether the flow carries an approval stage (phase 3 approval records).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.CanReopen">
            <summary>Whether Reopen is available at all — when true, the existing <c>ReopenPolicy</c> still decides each actual reopen.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.CanChangePriority">
            <summary>Whether the agent may change priority away from the request type's default.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind">
             <summary>
             The conceptual step kinds a <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep"/> can carry.
            
             <para>
             <b>Deliberately not a second <c>TicketStatus</c>.</b> The values that
             correspond to lifecycle states (<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Created"/>, <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.InProgress"/>,
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.PendingCustomer"/>, <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.PendingInternal"/>,
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Resolved"/>, <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Closed"/>) describe which existing
             <c>TicketStatus</c>/lifecycle event a step maps onto; the three that do
             not (<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Assigned"/> — the assignment dimension;
             <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Review"/>/<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.WaitingForApproval"/> — the approval
             concept) are workflow-step concepts realized as assignment/approval
             records over the unchanged status machine, per the phase decision to
             prefer approval records over new <c>TicketStatus</c> values. Nothing in
             this enum is written to <c>Tickets.TicketStatus</c>.
             </para>
             </summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Created">
            <summary>Ticket created — maps to <c>TicketStatus.Open</c>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Assigned">
            <summary>Assignment to a department/employee — the assignment dimension, not a status value.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Review">
            <summary>The responsible team reviews the request before work/approval — an approval-flow concept (Phase 3), not a status value.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.WaitingForApproval">
            <summary>Waiting on a configured approver (e.g. Accounting for Send Receipts, Customer Service for Handover) — an approval-record concept (Phase 3), not a status value.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.InProgress">
            <summary>Active work — maps to <c>TicketStatus.InProgress</c>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.PendingCustomer">
            <summary>Waiting on the customer (payment, documents, response) — maps to <c>TicketStatus.PendingCustomer</c>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.PendingInternal">
            <summary>Waiting on another internal department or an external party — maps to <c>TicketStatus.PendingThirdParty</c>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Resolved">
            <summary>The responsible team considers its work completed — maps to <c>TicketStatus.Resolved</c>.</summary>
        </member>
        <member name="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Closed">
            <summary>Customer Service considers the case completed — maps to <c>TicketStatus.Closed</c>.</summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate">
             <summary>
             A reusable workflow pattern a <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType"/> selects — never a
             per-request-type hard-coded flow. Three are seeded initially (Workflow/SLA
             Configuration phase 1): Standard (A), With Pending (B), With Approval (C).
            
             <para>
             <b>A template configures which of the existing lifecycle's transitions and
             actions are available; it does not define a new lifecycle.</b> The
             capability flags below gate the existing <c>TicketStatus</c> sub-machine
             (<c>Ticket.ChangeStatus</c>) and the approval concept: a template with
             <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AllowsPendingCustomer"/> false makes
             <c>InProgress → PendingCustomer</c> unavailable for request types using
             it (enforcement is phase 2); <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.RequiresApproval"/> marks the flow
             as carrying an approval stage realized as approval records (phase 3), not
             as new <c>TicketStatus</c> values. The ordered
             <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep"/> rows carry the same flow as displayable
             configuration for the workflow timeline/current-step UI.
             </para>
             </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.Code">
            <summary>Stable machine identifier (e.g. "STANDARD") — seed data and tests reference templates by this, never by generated id.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AllowsPendingCustomer">
            <summary>Whether flows on this template may use <c>TicketStatus.PendingCustomer</c> at all. A request type further restricts via <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.RequestType.AllowPendingCustomer"/> — see <see cref="M:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowCapabilities.Resolve(TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate,TigerCS.Domain.Modules.WorkflowConfiguration.RequestType)"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AllowsPendingInternal">
            <summary>Whether flows on this template may use <c>TicketStatus.PendingThirdParty</c> (internal department / external party). Same further restriction as <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AllowsPendingCustomer"/>.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.RequiresApproval">
            <summary>Whether this flow carries an approval stage (e.g. Accounting approval for Send Receipts, Customer Service approval for Handover) — approval records over the unchanged status machine, phase 3.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.Steps">
            <summary>The displayable step sequence, ordered by <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.Sequence"/>.</summary>
        </member>
        <member name="M:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AddStep(System.Byte,System.String,TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind,System.Boolean)">
            <summary>
            Appends the next step of the flow. Sequences must strictly increase so
            the stored order is the display order and can never be ambiguous.
            </summary>
        </member>
        <member name="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep">
            <summary>
            One displayable step of a <see cref="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.WorkflowTemplate"/>'s flow —
            configuration for the workflow timeline / "current workflow step" UI and,
            for <see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.Review"/>/<see cref="F:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind.WaitingForApproval"/>
            steps, the anchor the phase-3 approval records attach to. Steps never
            drive the status machine themselves: <see cref="T:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind"/>'s
            remarks explain the mapping onto the existing lifecycle.
            </summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.Sequence">
            <summary>Display order within the template. Strictly increasing per template (enforced by <see cref="M:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplate.AddStep(System.Byte,System.String,TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowStepKind,System.Boolean)"/> and a unique index).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.Name">
            <summary>Human-readable step name shown in the timeline (e.g. "Waiting for Accounting Approval") — never a technical event name.</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.IsOptional">
            <summary>True for steps a given ticket may skip entirely (e.g. the optional Pending Customer step of the With Pending template).</summary>
        </member>
        <member name="P:TigerCS.Domain.Modules.WorkflowConfiguration.WorkflowTemplateStep.WorkflowTemplate">
            <summary>Navigation back to the owning template — set by the template's own <c>AddStep</c>, never independently.</summary>
        </member>
    </members>
</doc>

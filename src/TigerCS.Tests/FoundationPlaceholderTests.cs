namespace TigerCS.Tests;

// Phase 1 foundation placeholder (S-02): proves the test project builds
// and runs under CI before any real unit tests exist.
public class FoundationPlaceholderTests
{
    [Fact]
    public void TestProjectRuns()
    {
        Assert.True(true);
    }
}

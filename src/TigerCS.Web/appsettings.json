{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Api": {
    "//": "Absolute base address of TigerCS.Api. Not a secret - an internal address, set per environment. No credential belongs in this file: every API call carries the signed-in user's own access token.",
    "BaseUrl": "",
    "Timeout": "00:00:30",
    "ResolveQueueSlaPerRow": true,
    "SlaResolutionMaxDegreeOfParallelism": 6,
    "DirectoryCacheDuration": "00:05:00"
  }
}
